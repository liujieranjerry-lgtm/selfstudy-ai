# Build An Agent From Scratch [2.5]：基础堆量的工作（知识版）

> 从“能跑”到“能干活”：Agent 能力边界的第一次扩展
> 原文：[Build An Agent From Scratch [2.5]：基础堆量的工作](https://www.tritium.work/2026/06/09/Build%20An%20Agent%20From%20Scratch%20%5B2.5%5D%EF%BC%9A%E5%9F%BA%E7%A1%80%E5%A0%86%E9%87%8F%E7%9A%84%E5%B7%A5%E4%BD%9C/)
> 发布于 2026-06-09，更新于 2026-06-11

## 这一章在讲什么

这是「从零搭建 Agent」系列的一个过渡章节。整个系列围绕一个核心对象展开：**Harness**——包住模型、驱动循环的工程系统，Agent 的能力边界最终由它决定。第 2 章搭好了其中最基础的部分：最小 **Agent Loop**，也就是感知—思考—行动的循环。系统能跑，但只能算个数、查个天气。第 2.5 章做的事情，是用一次“基础堆量”把 Agent 从玩具变成能接触真实世界的工具：横向接入多家模型服务，纵向补上文件、命令、网页三类工具，并顺手解决了工具输出可能塞爆上下文的问题。

理解这一章的关键，不在于它新增了几个工具，而在于它第一次完整展示了 Agent 的能力边界由什么构成。把文章内容收拢起来，可以总结成一个四要素框架：大脑、手、护栏、预算。模型是大脑，工具是手，安全与失败处理是护栏，上下文是预算。四者同时变化，才构成“Agent 能干活”这件事；缺了任何一环，能力都会变成风险。

本版只保留少量通用的小代码块用于解释概念，教学项目里的大段代码组合不展开；Codex 和 Pi 的完整案例放在文末。想看完整实现，对照原文或同目录下的详细版。

## 从“能跑”到“能干活”，还差什么

先回到上一章结束时的情况。第 2 章已经做到的事情，可以概括为一个能稳定运转的最小循环：统一的模型调用接口，一条感知—思考—行动的 Agent Loop，外加两个教学工具。这个系统证明了循环本身能跑通——模型能收到指令、决定调用工具、把结果作为观测继续推理，最后给出回答。

但它离“能干活”还差得很远，缺口恰好可以数成四个。

第一，大脑只有一个选择。系统只接了一家模型服务，模型涨价、限流或能力落后时没有退路。

第二，手只有两个玩具。计算器和查天气都无法触达真实任务材料，Agent 读不了文件、执行不了命令、获取不了实时信息。

第三，几乎没有护栏。工具结果都是干净的小文本，既没有失败需要处理，也没有权限和边界的概念；一旦换上真实工具，安全和失败恢复的问题会立刻出现。

第四，没有上下文预算。工具输出只有几行，系统还没面对过“一次任务产生大量信息”的压力。

这四个缺口，正好对应能力四要素：大脑、手、护栏、预算。接下来的框架，就是把它们放到同一张图里看——因为四者必须一起变化，Agent 才真正从“能跑”变成“能干活”。

## 能力边界：大脑、手、护栏、预算

| 要素 | 具体例子 | 一句话原则 |
| --- | --- | --- |
| 大脑 | 多 Provider、Provider Registry | 模型是可替换的组件，不绑定单一供应商 |
| 手 | 文件、Shell、Web 工具 | 能力来自工具集，工具要可配置、有边界 |
| 护栏 | 路径边界、失败即观测、权限预留 | 失败是信息，权限先于开放 |
| 预算 | 截断、offset/limit | 上下文是稀缺资源，任务复杂度有上限 |

四个要素必须放在一起看。只加工具不加护栏，Agent 会变成危险品；只换模型不换工具，能力没有实质提升；工具和模型都到位了却不考虑上下文预算，复杂任务会在中途失败。关于 Agent 能力边界的所有讨论，都可以回到四个问题：大脑够不够、手够不够、护栏有没有、预算撑不撑得住。

## 原则一：模型是可替换的组件（大脑）

如果一个系统只认某一家模型服务，供应商涨价、限流、下架、能力落后时，使用者只能被动承受。所以第一条原则是：把模型当作可替换的组件，而不是系统本身。

要做到这一点，核心逻辑只依赖统一接口，不接触任何一家供应商的细节；换模型时只换“适配层”，业务逻辑不动。供应商之间的差异（接口格式、工具调用写法、流式行为）被挡在边界外，Agent 主流程感知不到。

### 统一合同：LlmClient

一个通用的统一接口会长这样：

```typescript
export interface LlmClient {
  complete(request: LlmRequest): Promise<AssistantMessage>;
}

export interface StreamingLlmClient extends LlmClient {
  stream(request: LlmRequest): AsyncIterable<LlmStreamEvent>;
}
```

`LlmClient` 是一份“合同”：任何模型供应商都必须提供 `complete` 方法——接收统一格式的请求，返回一个“稍后到达”的答案（`Promise` 就是承诺，结果准备好后才兑现）；`StreamingLlmClient` 在这份合同上多加一条：还要支持 `stream`，把答案像水龙头一样一段一段流出来。核心逻辑只认这份合同，供应商的具体写法被挡在门外。

这条原则回答的问题是：切换模型后，同一个任务的表现是否一致、能不能对比；某个供应商不可用时，有没有降级路径、用户会不会感知。判断标准很简单——换一家模型，业务逻辑不用改；切换前后的体验可以评估和对比。

### Provider 与 LlmClient 的关系

**Provider 既不在 Agent Loop 里，也不属于工具系统，它和 LlmClient 是同一个边界的两段**——LlmClient 是“合同”，Provider 是“生产合同实现的地方”。

分层看是这样：

```text
Agent Loop（只认统一合同）
├── LlmClient（合同：complete / stream）
│        ↑ 由谁实现？
│        Provider 层：注册表 + 工厂（LlmProviderRegistry）
│          ├── openai-responses 的实现
│          ├── openai-chat 的实现
│          └── anthropic 的实现
└── Toolset（工具系统，完全平行的另一条线）
```

具体拆开说：**Agent Loop** 只依赖 `LlmClient` 这个接口。它拿到一个对象，调用 `complete(request)` 或 `stream(request)`，不关心对象内部是 OpenAI、Anthropic 还是别的什么。**LlmClient** 是合同本身。它不负责“选哪家”，只规定“任何模型供应商都必须长成这个形状”。**Provider** 是合同背后的“装配层”。它的工作有两件事：一是注册——用一张注册表（比如常见的 `LlmProviderRegistry` 这类实现）把 `openai-responses`、`openai-chat`、`anthropic` 这些 id 和各自的工厂函数对应起来；二是生产——根据环境变量（比如 API 密钥或 provider id）从注册表里取出对应工厂，工厂再产出一个具体的 `LlmClient` 实例，注入给 Agent。

### 三家 Provider

当前最主流的三个 Provider，覆盖了最常见的三类模型服务：

| Provider ID | 对应接口 | 典型用途 |
| --- | --- | --- |
| `openai-responses` | OpenAI Responses API | 最常用的默认路径 |
| `openai-chat` | OpenAI Chat Completions / compatible | OpenAI 兼容服务、本地 Ollama、第三方模型网关 |
| `anthropic` | Anthropic Messages API | Claude 系列模型 |

`openai-responses` 是最常见的默认路径；`openai-chat` 走的是 Chat Completions 接口，任何兼容 OpenAI 格式的服务（包括本地跑的 Ollama 和第三方模型网关）都能接进来；`anthropic` 对应 Claude 系列。这三家对工具调用的表示方式都不一样——同一个动作，A 家写一种格式，B 家写另一种——但 Agent Loop 不需要知道这些差异，它只面对统一的内部消息格式、工具调用格式和流式事件格式。这正是合同的用处：把差异挡在门外。

## 原则二：工具决定能力，也决定风险（手）

Agent 能做什么，由暴露给它的工具集（Toolset）决定。工具按需组合，能多能少，能力边界在启动那一刻显式定死，而不是运行中偷偷变化。这里有三类工具，每类都有自己的知识要点。

### 文件系统工具

文件工具是 Agent 获得持久化能力和长文本感知能力的基础：它能读材料、写结果、积累进度、查看目录。四个工具各司其职，但都必须被限定在一个活动范围内，不能越界访问；大文件应该分段进入，而不是一次全读；写和覆盖这类动作要特别小心，因为错误覆盖可能不可逆。

#### read_file：读文件

`read_file` 负责读取 UTF-8 文件内容，是 Agent 接触真实材料的第一道门。它的关键设计是支持 `offset` / `limit`——从第几行开始读、最多读多少——并且对超长输出做 head 截断（只保留开头），同时提示模型“还可以怎么继续读”。这意味着大文件不会被一次性塞进上下文，而是像看书一样先读一段，需要时再往下翻。Agent 由此获得长文本感知能力：它可以基于一份真实文档干活，而不是只能凭空回答。

#### write_file：写文件

`write_file` 负责写入并覆盖文件。两个细节值得注意：它会自动创建父目录（文件夹不存在时先建好），并返回写入的字节数（告诉调用方到底写了多少内容）。它的意义是 Agent 能把工作产物落盘——生成一份总结、一份代码、一份报告，用户能拿到真实结果。但“覆盖”这个语义也提示了风险：如果模型把旧文件覆盖了，损失可能不可逆，所以这类能力越强，越需要确认和版本保护。

#### append_file：追加内容

`append_file` 和 `write_file` 的区别在于它是“追加”而不是“覆盖”：往已有文件末尾继续写，同样会自动创建父目录和文件。它适合渐进式生成——比如让 Agent 一页一页写长文档、一天一天记日志。用覆盖会反复重写整个文件，用追加则每次只增加一小段，既省上下文又不容易丢历史。

#### list_directory：列目录

`list_directory` 负责列出目录内容，返回结构化 JSON，包含名称、类型、大小，并且会标明结果是否达到了 entry limit（条目上限）。它的作用很像人打开文件夹先看一眼：Agent 先知道目录里有什么、文件多大，再决定读哪个、怎么读。这个“先浏览再精读”的顺序，本身就是一种节省上下文的策略。

#### 路径边界：门禁

四个文件工具共用的保护是路径边界。先把目标路径换算成绝对路径，再检查它是否还在工作目录内，越界就直接抛错。比如模型试着读取 `{ "path": "../../.ssh/id_rsa" }`——`../` 表示向上跳出当前文件夹，这条路会试图溜出工作目录去读私钥文件——门禁会直接拦下，而不是让它得逞。这提醒我们：能力边界要清楚地表达给使用者，Agent 能读什么、不能读什么，应该透明可预期，而不是让人去猜。

### 系统执行工具：execute_command

`execute_command` 让 Agent 执行本地命令，是这一章里最强也最危险的工具：给 Agent 执行权，等于把一部分控制权交给它。它必须有安全阀（超时，防止无限阻塞），必须有结构化的失败信息（正常输出和错误输出分开，非零退出就是失败），并且失败后只保留最有价值的信息；破坏性操作（删除、覆盖、格式化）需要更严格的闸门。原文重点处理四件事：

| 机制 | 作用 |
| --- | --- |
| `timeoutMs` | 设置超时时间，防止命令无限阻塞 Agent Loop |
| stdout / stderr 捕获 | 让模型看到命令正常输出和错误输出 |
| exit code | 非零退出码标记为 `isError: true` |
| tail truncation | 命令失败时保留末尾更有价值的错误信息 |

超时是“安全阀”，防止一条命令把整个 Agent 卡死；stdout 和 stderr 分开，是把“正常结果”和“错误信息”分到两个抽屉，模型看得更清楚；退出码（exit code）相当于命令的“健康评分”，非零就说明出错了；tail 截断则利用了错误日志的规律——最有价值的信息通常在最后几行。命令失败后，模型看到的不是“程序崩了”，而是一张体检报告，比如：

```text
Exit code: 3
Timed out: false
Aborted: false
Wall time: 0.1 seconds

[stdout]
(empty)

[stderr]
bad
```

`Exit code` 是退出码（非零表示出错了）、`Timed out` 和 `Aborted` 说明有没有超时或中断、`[stdout]` 是正常输出、`[stderr]` 是错误输出。这份报告会被放回对话，让模型在下一轮自己决定怎么办——换一条命令、读一下文件、或者向用户上报。这是“能干活”和“危险”的分界线。

### 网络与搜索工具

网络能力被拆成两个小工具，而不是合成一个。原因很直接：搜索结果和网页正文的粒度完全不同——前者是“到哪里找”的线索列表，后者是“具体内容”——混在一起会让输出不可控。第三方搜索服务还带来成本、限流和来源可信度问题，需要提前定义边界和降级体验。

#### web_search：搜索

`web_search` 负责搜索实时信息，返回标题、URL、摘要和分数（相关性评分），帮模型找到候选信息源。它有一个很贴心的失败设计：缺少搜索服务的密钥（API Key）时，不是默默失败，而是返回一个模型可见的错误——模型会知道自己没有搜索能力，然后调整策略。这延续了“失败是一等公民”的原则：能力缺失也是信息，要明说。

#### fetch_url：抓取网页

`fetch_url` 负责获取某个 URL 的文本内容。抓取时先用一个解析 HTML 的工具去掉脚本和样式，只保留正文，然后做 head 截断。这相当于“去广告模式”：网页里有大量导航、脚本、样式噪声，Agent 真正需要的是正文本身。抓取和搜索分离后，搜索负责指路，抓取负责阅读，每一步的输出都是可控的。

当所有工具都打开后，一次典型的长任务会让 Agent 连续经历这样的链路：

```text
read_file
  -> web_search
  -> fetch_url
  -> write_file
  -> final answer
```

也就是从“调用一个工具”变成“完成一条多工具任务链”。任务链越长，每一步产生的信息都会进入历史，上下文压力也随之而来——这正是后面护栏和预算要解决的问题。

## 原则三：失败是信息，权限先于开放（护栏）

整篇文章里最容易被忽略、却最影响体验的一点，是把失败当成一等公民来设计。这里说的护栏有两半：一半是“失败怎么处理”，另一半是“权限怎么控制”。它们共同决定了 Agent 能力开放之后，系统还安不安全、可不可信。

### 失败不是事故，而是信息

工具调用失败不会炸掉整个循环，而是被包装成一个 **Observation**——工具执行的结果，既包括成功输出也包括失败信息——再用 **isError** 标记出来，放回对话历史，让模型在下一轮看到并反思修正。Codex 给我们的启发也在这里：工具错误不能直接炸掉 Loop，任何调用失败都应该变成模型可见的 observation，让模型继续反思修正，而不是把问题吞掉或直接崩溃。

### 失败怎么被打包

要让模型“看得见”失败，失败必须被打包成结构化信息，而不是一句“出错了”。前面那份“体检报告”（Exit code、Timed out、Aborted、stdout、stderr）就是打包之后的模样：退出码说明出了什么错，超时和中断说明失败方式，正常输出和错误输出分开存放，模型一眼就能判断下一步该做什么。

无论哪个系统，打包失败时都有三个不变的要求。第一，有一个明确的成功/失败标记，让程序和模型都能快速判断这次调用成没成。第二，失败时带着可供查看的具体信息——命令是什么、错在哪、输出是什么——而不是只有一句“失败”。第三，失败不会让循环崩溃，而是作为一条普通消息回到模型面前，让模型继续决策。

字段名会因系统而异：标记可能叫 `isError`，也可能叫 `is_error`、`ok`、`status`；附件信息可能叫 `details`，也可能叫 `metadata`、`diagnostics`。变的是名字，不变的是这三条要求。

### 模型怎么恢复

这背后是一个重要的观念：Agent 的可靠性不靠“不犯错”，而靠“犯错之后能恢复”。用户最终信任的不是 Agent 从不失败，而是失败之后它能给出合理的下一步——换一个命令、读一下文件、换一条路径，或者诚实地上报。当然，恢复不等于无限重试：每次失败都会重新消耗模型调用和上下文空间。所以一个成熟的设计会明确重试次数、上报策略和退出路径，并决定失败过程对用户是否可见、可见到什么程度。值得留意的是：失败信息会进入上下文吗？重试会重复计费吗？用户能看到失败原因和 Agent 的修正过程吗？

### 权限与沙箱：护栏的另一半

失败处理解决的是“出了问题怎么办”，权限控制解决的是“不该做的事能不能做”。Codex 用审批和沙箱实现工具安全可控调用：调用前先过审批，危险操作放进隔离环境。这一章还没有实现完整的调用审批，只留了扩展的点，所以它明确属于“下一批要做的事”。这也是为什么“开放程度”和“治理强度”必须放在一起讨论：工具越强，白名单、审批、沙箱就越要跟上。

## 原则四：上下文是预算（预算）

Agent 接触真实世界后，一次任务的信息量会急剧增长：文件内容、命令输出、网页正文都在抢占有限的上下文。所以上下文不是技术参数，而是预算。它决定三个问题：一次任务最多能吃进多复杂的内容？一次任务的上限成本是多少？超限之后用户得到什么体验？这一节把它拆成三块：往哪个方向截、截多长、截完之后怎么继续。

### 往哪个方向截：truncateHead 与 truncateTail

截断不是随便砍，而是有方向的选择：

| 截断方式 | 用在哪里 | 为什么 |
| --- | --- | --- |
| `truncateHead`（留头） | 文件读取、目录列表、网页正文 | 先看开头，适合正文和结构化列表 |
| `truncateTail`（留尾） | shell 命令输出 | 错误日志通常越靠后越有价值 |

文件、目录、网页这类内容，开头通常最有信息量，所以保留头部；命令输出的错误信息往往在末尾，所以保留尾部。方向选对了，截断之后留下的才是模型最需要的内容。

### 截多长：长度红线

输出要有长度红线，超过就截断，不会无限增长。红线可以按行数设，也可以按字节数设（字节可以粗略理解成“字符的度量单位”），很多系统两种都设。具体数值不是技术标准，而是产品决策：给模型看多大的文件、留多少空间，取决于任务类型和成本预算。

### 截完之后怎么继续：offset / limit

截断不是悄悄丢弃，而是明确告诉模型还剩多少、怎么继续。比如读文件时使用 `offset` / `limit`：

```json
{
  "path": "logs.txt",
  "offset": 200,
  "limit": 80
}
```

这相当于说“翻到日志的第 200 行，从那里开始读 80 行”。如果内容被截断，模型会收到这样一张便条：

```text
[Truncated: showing lines 2-4 of 5; 13B/23B selected bytes. Use offset=4 to continue.]
```

翻译成人话：5 行里我给你看了第 2 到 4 行，23 个字节里选了 13 个字节，想继续读就用 `offset=4` 接着来。大文件不一次性塞进上下文，而是分段读取、摘要、再决定要不要继续——这实际上是把“阅读策略”做进了能力里，就像看书时先翻目录、再看章节、最后才精读。

判断标准：任务超限后不是无声失败，而是可继续、可降级、可解释。

## 三个关键认识

第一，能力扩展必然带来上下文压力。工具越多、任务链越长，信息量增长越快，预算问题会提前暴露。所以设计复杂 Agent 功能的第一天，就要为上下文预算留位置。

第二，失败处理是体验设计，不是内部事务。失败怎么被记录、怎么被看见、怎么被用户感知，直接塑造信任。把失败流程当作一条用户路径来设计，是 AI 产品和传统软件的重要区别。

第三，开放程度与治理强度必须同步规划。只展示 Agent 能做什么不够，还要说清楚它不能做什么、以及如何保证这一点——这既是安全要求，也是信任的来源。

## 一组可以带走的问题

以后见到任何“让 Agent 干活”的系统，可以用这组问题检验：

- 它需要哪些工具？最小工具集是什么？
- 每个工具的风险等级如何？有没有白名单、审批或沙箱？
- 一次完整任务最坏会产生多少上下文？成本上限是多少？
- 失败之后用户看到什么？重试几次？什么时候退出？
- 模型是否可以替换？切换后同一任务的体验是否一致？
- Agent 的能力边界是否透明？做不到的事情如何表达？

## 两个真实案例：Codex 和 Pi

文章作者在动手前拆解了两个真实产品：OpenAI 的 Codex 和轻量框架 Pi。它们不是知识本身，而是知识最好的例证——前面讲的每个原则，都能在它们身上看到具体模样。

### Codex：模型供应商是资料卡，工具是受治理的运行时

Codex 用一张“供应商资料卡”描述一家模型服务：除了服务地址和密钥位置，还包括接口格式、请求重试次数、流式输出重试次数、输出卡住多久算超时、要不要附带额外请求头、用什么方式鉴权、是否支持特殊云通道、是否支持实时双向连接。

```text
wire_api
request_max_retries
stream_max_retries
stream_idle_timeout_ms
http_headers
env_http_headers
auth
aws
supports_websockets
```

这些字段放在一起就是一张资料卡。换一家供应商，就换一张卡，主程序完全不用知道卡里的细节——这就是“模型是可替换的组件”的具体模样，就像换手机运营商只需要换 SIM 卡。

Codex 的工具系统则示范了“治理”：工具不止有名字和执行，还有统一契约、调用前后的检查点（hook）、审批流程和调用记录。它用行动说明了三条原则：工具错误不能炸掉整个流程，失败要变成模型可见的信息；工具调用要能被权限管控；供应商和工具都不能写死在核心循环里。

### Pi：模型用“登记簿”管理，工具都装“安全带”

Pi 管理模型的方式是一张登记簿：

```typescript
const apiProviderRegistry = new Map<string, ApiProvider>();

export function registerApiProvider(provider: ApiProvider): void {
  apiProviderRegistry.set(provider.api, provider);
}

export function getApiProvider(api: Api): ApiProvider | undefined {
  return apiProviderRegistry.get(api);
}
```

`apiProviderRegistry` 是登记簿本身（名字到内容的对照表）；`registerApiProvider` 负责登记，把供应商标识和供应商实现对应起来；`getApiProvider` 负责查询，按标识取出实现。想接入新供应商，只需要登记，不需要改主程序——这是“模型可替换”最直接的示范。

Pi 的工具则普遍装着“安全带”：读文件支持分段读取（从哪一行开始、最多读多少），超长内容按行数或字节数截断，并提醒模型大文件要继续分段读；执行命令有超时、正常输出和错误输出分开记录、中断时清理干净、超长输出截断。能力越强，每一步越要有约束——这是“护栏”原则的缩影。

### 案例给我们的结论

两个案例合起来说明一件事：成熟的产品不是能力更强，而是把能力、边界和治理一起做进架构。这也是为什么四要素必须一起看。

## 延伸与原文

这一章把 Agent 接到了真实世界，也把“上下文压力”正式摆上台面。下一章 Context Manager 要回答的正是预算问题：每一轮模型该看到什么、保留什么、截断什么、摘要什么。这个话题会越来越接近成本与体验的权衡。

本文依据[原文](https://www.tritium.work/2026/06/09/Build%20An%20Agent%20From%20Scratch%20%5B2.5%5D%EF%BC%9A%E5%9F%BA%E7%A1%80%E5%A0%86%E9%87%8F%E7%9A%84%E5%B7%A5%E4%BD%9C/)整理；本版只讲知识，实现细节见同目录下的详细版和原文项目 [Singularity](https://github.com/Tritium0041/Singularity)。
