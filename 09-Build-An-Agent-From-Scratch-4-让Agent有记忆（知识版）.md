# Build An Agent From Scratch：让 Agent 有记忆（知识版）

> 让 AI 助手「记住」该记住的事。
> 原文：[Build An Agent From Scratch 4：让 Agent 有记忆](https://www.tritium.work/2026/06/17/Build%20An%20Agent%20From%20Scratch%20%5B4%5D%EF%BC%9A%E8%AE%A9%20Agent%20%E6%9C%89%E8%AE%B0%E5%BF%86/)
> 本版只讲通用知识，不复制教学项目的实现；少量代码只是「合同或结构化输出」的例子，完整实现见文末原文。

## 结论先行

AI 模型（尤其是大语言模型，也就是 ChatGPT 这类能理解和生成文字的模型）并没有天生的记忆。它每一轮能「看到」的内容是有限的，这个有限的视野叫做上下文（context），可以把它想象成一张正在使用的桌面：对话里出现过的东西一旦被移出桌面，模型就看不到了。当任务很长时，系统往往会把旧对话压缩成摘要，这个动作叫做上下文压缩；而压缩是有损的——一些曾经出现的事实会从此消失。

这正是「为什么管理好每轮看到什么之后，还需要记忆系统」：压缩解决的是桌面太挤的问题，记忆解决的是「重要事实不能因为压缩而丢」的问题。

于是，一个合格的记忆系统要回答三个问题：哪些事实值得从眼前的内容里提取出来？这些事实应该留在当前任务里，还是长期保存？需要的时候，怎样把它们找回来？

这篇文章给出的核心答案可以概括成一句话：**把记忆分成三层（眼前、当前任务、长期），给每一层配上写入与读取的工具（工具就是模型可以主动请求执行的外部动作，比如读写文件、搜索、执行命令）和使用规则，并坚持一条总约束——模型不许假装自己记得，一切以工具实际取回的结果为证据。**记忆不是让模型「记性好」，而是给模型一间可靠的档案室：需要时去查，查到的才算数。

## 核心框架：三层记忆与三条护栏

把结论里的核心答案拆开，就是一套可以按顺序建造的结构：先给记忆分好房间，再给每个房间立规矩。先看整体框架，再按建造顺序逐条看原则。

**三层记忆**：

| 层 | 生命周期 | 保存什么 | 什么时候用 |
| --- | --- | --- | --- |
| 短期记忆（当前上下文） | 从当前这一步到最近几步 | 正在进行的对话、工具调用与结果、压缩后的摘要 | 每一轮都在用，用来完成眼前这一步 |
| 中期记忆（工作区笔记） | 当前任务从开始到结束 | 阶段性结论、重要文件、错误记录、待办事项、已做的设计决定 | 任务过程中随时查看，不随压缩消失 |
| 长期记忆（记忆库） | 跨任务、跨会话 | 用户偏好、项目约定、可复用经验 | 只有相关内容被召回时才进入视野 |

三层必须一起看：它们不是可选项，而是分工。**短期记忆管「现在看到什么」，中期记忆管「这个任务做到哪了」，长期记忆管「下次还适用什么」。**缺任何一层，要么任务中途忘事，要么把全部旧信息都塞回桌面、把注意力搅乱。

**三条护栏**：

贯穿这套系统的还有三条护栏，它们会在建造的不同阶段依次登场：

- 读写分离：读取要轻、要频繁、要可控；写入决定什么值得长期保留，更危险。
- 可解释：每次召回（也就是把存好的记忆按需找回来的动作）都要能说清「为什么命中、返回了什么」。
- 克制：有记忆不等于每轮都看全部记忆；召回要少量、按需、不喧宾夺主。

下面十一条原则，就是按建造顺序排的：先分房间，再定准入与写入的规矩，然后选容器、定取回的方法，最后立使用说明、划清边界。每一条都踩在前一条已经成立的基础上。

## 原则一：记忆要分层，按生命周期分仓库

整套系统是从「分房间」开始的：在决定存什么、怎么存之前，先决定信息放在哪一层。

原则是什么：把模型接触到的信息按「还能活多久」分成三层：眼前（当前上下文）、当前任务（工作区笔记，可以理解为这个任务专用的草稿本）、长期（记忆库）。每一层的存放位置、写入方式、召回方式都不同，不能混在一起。

它回答什么问题：一条信息该存多久、放哪里、什么时候取回。

判断标准：先问「这条信息还需要吗」。完成眼前这一步需要的，留在当前上下文；当前任务后续还用得上的（阶段性结论、关键错误、待办、已做的决定），写进任务笔记；跨任务、跨会话也用得上的（偏好、约定、经验），才进长期记忆。分层错误最常见的两种表现，是把一次性细节写进长期记忆，或者让任务级的结论跟着压缩一起丢。

## 原则二：记忆有边界，只存「未来还会用」的东西

房间分好了，接下来的问题是准入：什么样的东西才有资格进长期那间房？

原则是什么：长期记忆只保存三类东西：用户偏好、项目约定（做事的稳定方式）、可复用经验（反复踩坑后的教训）。明确不保存的包括：一次性任务细节、临时搜索结果、密钥与凭据、能从原处重新读到的内容。

它回答什么问题：什么值得跨会话保存？

判断标准：一句话——「下一次任务还用不用得上」。用得上且稳定，才进长期记忆；一次性事实是噪音，密钥凭据是安全风险，随时能重新读到的文件内容存下来只会过时。原文给长期记忆工具写的边界说明，本身就是一份很好的「合同」：
```text

Store durable long-term memory as a local Markdown entry.
Only save user preferences, project conventions, or reusable lessons.
Never store secrets, credentials, or one-off temporary facts.
```

这段文字在系统中的角色，是给「写入」这个动作立规矩：它不是代码逻辑，而是工具对模型的行为约定，把允许存什么、禁止存什么一次说清。按执行顺序看，模型在写入前先对照三类「允许」清单，再对照三类「禁止」清单，都通过才落笔。它重要的原因是，长期记忆一旦被污染就难以清洗——一条错误的旧记忆会在未来无数次任务中被反复召回，所以入口处的边界比任何事后清理都有效。

| 允许保存（Only save） | 禁止保存（Never store） |
| --- | --- |
| 1. 用户偏好：你明确说过喜欢/不喜欢的东西，比如「示例都用 TypeScript 写」 | 1. 密钥：API key、token、密码这类一旦泄露就危险的信息 |
| 2. 项目约定：这个项目做事的稳定方式，比如「Markdown 成稿默认不进 git」 | 2. 凭据：cookie、登录态、授权信息等证明身份的东西 |
| 3. 可复用经验：反复踩坑后总结的教训，比如「这类任务先搜索再动手」 | 3. 一次性临时事实：只对当下这一次任务有用的细节，比如某次搜索的结果、某个临时结论 |

## 原则三：读写分离，读要轻、写要慎重

准入规则划清了「能存什么」，但写入这件事本身比读取更危险：读错了可以重读，写错了会污染整间档案室。所以读和写必须分开设计。

原则是什么：读取记忆和写入记忆不是同一件事，也不该是同一个动作。读取发生在任务各个阶段，应该轻量、频繁、结果可解释；写入意味着系统要判断「什么值得长期保存」，这一步更危险，也更容易污染记忆库。

它回答什么问题：为什么记忆工具不能只有一个「存/取」按钮？

判断标准：读的入口可以随时调用、开销小、看得到结果；写的入口必须有明确的边界说明（什么能存、什么不能存）。随手翻档案查资料很安全，往档案室归档新文件前，得先想清楚这份文件值不值得留。

## 原则四：写入要防污染，空笔记比没有笔记更糟

读写分开之后，还要守住写入的卫生：一条空笔记、半截笔记混进档案室，比没有笔记更糟。

原则是什么：记忆库最怕被空内容、半截内容、无意义内容慢慢填满。写入前要做规范化：内容必须非空、类别必须合法；同时，系统内部状态与外部可见的副本要分离，外部不能绕过校验直接改内部数据。

它回答什么问题：如何让记忆库长期保持干净可信？

判断标准：**每条记忆都有固定结构——编号、类别、内容、创建与更新时间；空内容在入口处就被拒绝；外部拿到的都是拷贝，修改必须走受控通道。**档案室的每份文件都要有封面和日期，白纸不归档，外人也不能直接往档案柜里塞纸。

下面的例子是一条「任务笔记」的结构合同（原文把它称为 WorkspaceNote，意思是工作区里的一条笔记）：
```typescript

type WorkspaceNote = {
  id: string;
  kind: "note" | "decision" | "file" | "error" | "todo";
  content: string;
  createdAt: string;
  updatedAt: string;
};
```

它在系统中的角色，是规定「中期记忆的每一条笔记必须长什么样」：编号、类别、内容、创建时间、更新时间，一个都不能少。按执行顺序看，先分配编号，再定类别，然后写入内容并盖上时间戳；读取时则按编号或类别过滤。它重要的原因是，**固定结构让写入可以被校验、读取可以按类过滤、界面可以分门别类展示——「阶段性结论」「错误记录」「待办」不会混成一锅粥。**

## 原则五：存储格式要可读、可审计、可搜索

房间、准入、读写、卫生都定了，接下来要选容器：长期记忆用什么格式装，才能既轻又可靠？

原则是什么：长期记忆可以用人类可读的纯文本格式保存（原文用的是 Markdown——一种用纯文本写成、带简单标记的文件格式）。每条记忆是一个带元数据的结构化块：编号、标签、来源、创建与更新时间，下面是正文。选这种格式的理由是：实现轻、不需要数据库迁移、人可以随时打开检查、模型也能直接搜索。

它回答什么问题：记忆库用什么「容器」来装？

判断标准：当记忆规模不大时，可读性、可审计性、可搜索性、低维护成本优先于复杂技术；不要为了「先进」引入需要迁移和运维的存储。**判断一种存储方式是否合格，问三个问题：人能看懂吗？变化能被检查吗？需要时能搜到吗？技术会变，格式简单反而活得久。**

一条长期记忆的档案格式示例如下：
```markdown

## mem_20260613_201530_ab12
- tags: typescript, preference
- source: user
- created_at: 2026-06-13T20:15:30.000Z
- updated_at: 2026-06-13T20:15:30.000Z
User prefers TypeScript for scripts.
```

这段内容在系统中的角色，是长期记忆的「单页档案」。按执行顺序看，写入时先生成带时间的编号，再填元数据（标签、来源、时间），最后写正文；检索时先按标签过滤，再从正文里截取摘录。它重要的原因是，元数据让检索有方向，正文才是真正的内容，而纯文本格式让整份档案既可以被人读，也可以被程序直接搜索——审计与召回都变得很轻。

## 原则六：召回要有方向：筛选、打分、给摘录

东西存进去了，最难的部分才刚开始：需要时怎么把它捞回来？答案不是倒出整间仓库，而是有方向地捞。

原则是什么：找回记忆不是把所有条目倒出来，而是**先用查询缩小范围（例如按标签过滤，标签就是给记忆贴上的分类词），再对不同类型的命中给不同权重，排序后只返回最相关的少数几条；返回的摘录（snippet，即内容片段）应来自记忆正文，而不是来自标签这类辅助信息。**否则搜「偏好」只会看到「标签：偏好」，看不到用户真正说过的话。

它回答什么问题：需要一条记忆时，怎么精确捞到它，而不是捞回一大堆？

判断标准：检索结果有排序、有数量上限、命中理由可解释；摘录展示真实内容而非元数据（元数据就是描述数据的辅助信息，如标签、时间、来源）。原文用关键词检索举例：整句命中权重最高，标签命中次之，单词命中最低，再按分数和更新时间排序——这个打分只是「例如」，很多系统改用语义相近度检索，但原则不变：先缩小范围、再排优先级、只返回最相关。

一个典型的「请求—应答」合同长这样。请求先给出查询词、可选标签和数量上限：
```json

{
  "query": "TypeScript script preference",
  "tags": ["preference"],
  "maxResults": 3
}
```

应答把每条命中连同一套证据一起返回：内容、标签、来源、分数、命中的词、摘录：
```json

{
  "results": [
    {
      "id": "mem_...",
      "content": "User prefers TypeScript for scripts.",
      "tags": ["typescript", "preference"],
      "source": "user",
      "score": 8,
      "matchedTerms": ["typescript"],
      "snippet": "User prefers TypeScript for scripts."
    }
  ]
}
```

这段「请求—应答」在系统中的角色，是检索这个动作的合同：调用方先说清要找什么，系统只回最相关的几条。按执行顺序看，请求里显式写了数量上限（maxResults），系统据此裁剪结果，再为每条结果附带来源、分数和摘录。它重要的原因是，请求端的「上限」体现了预算思想——召回也要克制；应答端的「来源与命中词」体现了可解释原则——模型引用记忆时，人能顺着这些字段验证它有没有用对。

## 原则七：记忆要可解释，每次召回都能归因

捞回来的东西，凭什么让模型信、让人信？答案是一次召回必须能说清「为什么命中、返回了什么」。

原则是什么：一次记忆召回，要能说清楚「为什么命中、返回了什么内容」。当模型引用一条记忆时，人应该能顺着检索依据、来源、标签，理解它为什么这么想。

它回答什么问题：如何防止模型把记忆用成一个不可追问的黑箱？

判断标准：召回结果带来源（例如这条记忆是用户提供的，还是系统记录的）、标签、命中依据和内容摘录；当模型说「我记得你偏好某样东西」时，能回答「这条记忆从哪来、凭什么相关」。如果只能看到结论、看不到依据，这条召回就不合格。

## 原则八：上下文是预算，记忆是库存

单条召回能解释还不够，还要管住总量：上下文是预算，记忆是库存——预算有限，库存再大也不能一次全摆上桌面。

原则是什么：模型每一轮能「看到」的内容是有限的，就像一张容量固定的桌面；历史、工具结果、召回的记忆都在争夺这张桌面。记忆系统的作用不是把所有记住的东西都摆上桌面，而是把记忆放在桌外的仓库里，按需取用少量。这背后是一个贯穿始终的比喻：上下文是预算，记忆是库存——预算有限，库存再大也不能一次全拿出来。

它回答什么问题：如何避免「记得越多，干活越差」？

判断标准：每一轮进入桌面的召回结果有数量上限；旧偏好、旧经验默认不出现，只在相关时被召回来；记忆内容一旦进入上下文，就和普通工具结果一样接受截断（把过长内容按规则裁短）、压缩与预算管理，没有特权通道。判断系统是否克制，就看一个现象：任务进行中，桌面上会不会突然堆满与当前步骤无关的旧记忆。

## 原则九：不假设记忆存在，查到的才算证据

找得到、信得过、不贪多——最后还有一条更硬的总约束：不假设记忆存在，查到的才算证据。

原则是什么：模型不能因为「系统里装有记忆工具」就假装自己记得。它必须实际调用工具、拿到结果，才能把结果当作证据使用。这是全文最核心的一条约束： Do not assume memory exists. Treat only tool results as retrieved memory evidence（不要假设记忆存在；只有工具实际取回的结果，才能当作记忆证据）。

它回答什么问题：如何防止模型编造记忆、张冠李戴？

判断标准：任何被当作记忆使用的信息，都能追溯到一次实际的检索或读取动作；凭印象、凭提示词暗示，都不算。提示词就是每轮对话开始前固定给模型的开场说明；记忆的正确形态不是「模型永远记得」，而是「模型有可靠的方式在需要时去查」——记忆是一组外部状态接口，不是一个魔法提示词。

## 原则十：工具要配使用说明，模型才知道何时该用

取回与验证的规矩都齐了，但模型怎么知道「现在该不该调用记忆」？光有档案室还不够，还得有一份使用说明。

原则是什么：只有工具还不够，还要有稳定的使用说明：搜记忆要发生在「凭印象」之前；看清单要发生在「读全文」之前；写入只写该写的。说明本身固定不变，不随具体对话变化，每轮对话开始前放进系统的开场说明里（也就是系统提示词）。

它回答什么问题：模型怎么知道「现在该不该调用记忆」？

判断标准：每个记忆工具都有明确的使用时机与边界；提示里写明「先搜再信」「先看清单再看全文」「写入只写持久信息」，并且提醒不要假设记忆存在。把「什么时候用」变成可执行规则，而不是留给模型自由发挥。光有工具、没有使用说明的系统，就像一间没人告诉你怎么用的档案室——你知道档案在，但不知道该查哪份、什么时候查。

## 原则十一：会话记录不等于长期记忆

到这里，记忆系统内部的构造已经完整。最后要划清一条外边界：能恢复对话不等于有记忆，会话记录不等于长期记忆。

原则是什么：至少存在三种不同的「保存」：运行中的对话历史（当前这一轮的完整对话）；可恢复的会话记录（会话就是一次连续的对话；会话记录把某次对话连同任务笔记存下来，之后可以回去继续）；跨会话的长期记忆（不依附于任何一次会话，下一次任务也要读）。它们的生命周期不同、用途不同，不能混为一谈。

它回答什么问题：为什么「能恢复对话」不能替代「记忆」？

判断标准：判断一次保存属于哪种——跟着某次会话走的是会话记录；独立于会话、下次任务也要读的是长期记忆。能回到旧对话，让长期记忆有了用武之地；但恢复会话只是「回到现场」，长期记忆才是「把经验带走」。给会话起个短标题、让列表更好浏览，这类工作只属于整理会话的辅助功能，不属于记忆本身。

三种保存的对比如下：

| 保存形式 | 生命周期 | 用途 |
| --- | --- | --- |
| 运行中的对话历史 | 当前任务进行期间 | 支撑当前每一步的回答 |
| 会话记录 | 某一次会话的存续期间 | 让对话可以恢复、切换、继续 |
| 长期记忆 | 跨会话 | 让偏好与经验被下一次任务使用 |

## 三个关键认识

十一条原则垒完了，把整座房子收束成三个可以带走的认识。

第一，记忆是外部状态，不是模型的内在属性。模型不需要假装永远记得，而是需要一个可靠的方式去查；只有查回来的结果才算数。这决定了记忆系统的基本形态：工具加规则加存储，而不是一段更长的提示词。

第二，上下文是预算，记忆是库存。预算有限，库存再大也要分层存放、按需取用、克制召回；所有进入桌面的内容，包括记忆，都接受同一套管理。系统设计得好不好，看的是取用是否克制、可解释、有边界，而不是记得多不多。

第三，记忆系统的成败在规则，不在技术。什么值得存、什么绝不能存、何时读、何时写、凭什么信，这些边界问题决定了记忆是否可信、是否干净；至于存储用文本文件还是数据库、检索用关键词还是语义匹配，都是次要选择。

## 一组可以带走的问题

认识是给自己看的，清单是拿去用的。动手设计或评价一个记忆系统时，可以拿这组问题过一遍：

- 这条信息属于哪一层：眼前这一步、当前任务，还是未来任务？
- 它值得长期保存吗？下一次任务还会用吗？还是只是一次性的？
- 它是不是敏感信息？密钥、凭据、临时细节是否被排除在长期记忆之外？
- 模型是在「凭印象」还是在「查档案」？任何被当作记忆的证据，都能追溯到一次实际的检索或读取吗？
- 召回结果可解释吗？能说清为什么命中、返回了什么、来源是什么吗？
- 每一轮召回的条目是否有限量？旧记忆会不会挤占当前任务的注意力？
- 写入通道防污染吗？内容非空、类别合法、修改必须走受控接口？
- 记忆内容进入上下文后，是否与普通工具结果一样接受截断、压缩与预算管理？
- 存储格式可读、可审计、可搜索吗？还是变成了一个黑匣子？
- 使用说明是否写清了每个记忆工具「何时用、何时不用」？

## 真实案例：原则如何落地到工程

清单问的是「有没有」，案例看的是「长什么样」。以 Codex 为例。它的长期记忆做法是：把项目约定与用户偏好写进项目目录里一份可读的 Markdown 文件，让每个新会话都能读到，而不是塞进模型参数或某次对话的历史里。

这个案例示范了三条原则。一是原则五：存储用可读、可审计、可搜索的文本格式，人可以直接打开检查，Agent 也可以直接读取。二是原则二：文件里只放项目约定、用户偏好这类「未来还会用」的内容，不放一次性事实。三是原则九：Agent 每次都以实际读取文件的结果为准，而不是凭印象声称知道约定。这也解释了为什么很多 AI 编码助手都采用「项目根目录放一份约定文件」的模式：那份文件既是给人看的文档，也是给 Agent 看的记忆。

## 延伸与原文

本版是通用知识版：只讲脱离具体项目也成立的原则，删去了完整类实现、组装流程、启动方式、环境变量与具体参数。知识版和详细版可以并存——如果你想要可运行的设计与实现，请直接阅读原文。

原文：《[Build An Agent From Scratch 4：让 Agent 有记忆](https://www.tritium.work/2026/06/17/Build%20An%20Agent%20From%20Scratch%20%5B4%5D%EF%BC%9A%E8%AE%A9%20Agent%20%E6%9C%89%E8%AE%B0%E5%BF%86/)》，这是「Build An Agent From Scratch」系列的第四篇，上一篇讲上下文引擎，下一篇讲任务规划。
