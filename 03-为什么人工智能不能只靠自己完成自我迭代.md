# 03 — 为什么人工智能不能只靠自己完成自我迭代

> 本文是 selfstudy-ai 自学笔记的第 3 篇。<br>
> 目标：分清自我修正、自我训练和递归式自我改进，并理解为什么 AI 不能只靠自己的输出，持续完成可靠的能力升级。

---

## 一、先记住一句话

> **AI 可以反复修改自己的答案，但修改答案不等于升级自己；AI 也可以学习自己生成的数据，但没有外部信息和可靠反馈，它只会复制已有能力，并逐渐放大已有错误。**

平时我们经常听到一种说法：

> AI 已经能够反思自己的答案，还能生成训练数据，所以它迟早可以摆脱人类，独立完成无限次自我迭代。

这句话把三件不同的事情混在了一起：

1. 在一次回答中检查并修改答案。
2. 用模型生成的数据继续训练模型。
3. AI 改进自己的模型、算法和训练流程，再用改进后的自己继续改进。

第一件事已经很常见，第二件事也已经可以做到，第三件事则仍然只是部分实现。它们不能统称为同一种“自我迭代”。

---

## 二、自我修正不等于自我迭代

假设模型第一次回答一道题，得到：

```text
答案：2.8
```

随后我们让它“检查一下”，模型重新推理，改成：

```text
答案：3
```

第二次回答比第一次好，但这通常只发生在当前对话的推理过程中。模型重新读取了题目、自己的旧答案和新的提示，然后又执行了一次前向传播。

在这个过程中：

- 模型的参数通常没有改变；
- 模型结构没有改变；
- 训练数据没有改变；
- 对话结束后，它不一定永久学会了这次修正。

因此，这更准确地说是**推理时的自我修正**，或者**推理时迭代**，不是模型真正升级了自己。

可以把两者画成下面这样：

```text
自我修正：
同一个模型参数 θ
  → 第一次回答
  → 检查答案
  → 第二次回答

真正迭代：
旧模型参数 θ₁
  → 训练或改进
  → 新模型参数 θ₂
  → 以后面对其他问题时，能力也发生了稳定变化
```

所以，判断模型有没有完成真正的迭代，不能只看它有没有把当前答案改对，还要看它的参数、训练方法、工具使用能力或整个系统能力是否发生了可以保留下来的变化。

---

## 三、真正的自我迭代，至少要改变模型本身

第 2 课已经讲过，模型可以写成：

```text
ŷ = f(x, θ)
```

其中，f 表示模型结构和计算规则，x 是输入，θ 是通过训练得到的参数，ŷ 是模型给出的预测结果。

如果模型只是在相同的模型结构 f 和参数 θ 下多计算几次，那么变化的是本次计算过程，不是模型本身。

真正的能力迭代至少要出现以下一种变化：

- 参数从 θ₁ 更新为更好的 θ₂；
- 模型结构或训练算法得到改进；
- 系统学会了新的工具或新的行动策略；
- 新能力能够被保存，并迁移到之后的其他任务。

但只要涉及训练，就会立刻遇到两个问题：

1. 下一轮训练使用什么数据？
2. 谁来判断这些数据和答案是对是错？

这两个问题决定了 AI 能不能只靠自己完成有效迭代。

---

## 四、只学习自己的输出，为什么会越来越蠢

先看一个最简单的例子。

假设一道题的标准答案是：

```text
y = 3
```

当前模型已经能够生成答案 3。现在让它批量生成训练数据，再用这些数据训练下一代模型。

### 情况一：模型每次都生成 3

```text
3、3、3、3、3……
```

这些数据没有提供超出当前模型的新知识。模型只是把自己已经会的答案重复了很多遍。

重复正确答案可能让下一代模型更稳定地记住这个答案，但它不能仅凭这些重复内容知道原模型不知道的东西，也不能证明自己在其他题目上变得更聪明。

换句话说：**模型可以复制自己的能力上限，却不能从完全相同的信息中可靠地创造出超过这个上限的新知识。**

### 情况二：模型偶尔生成 2.8、2.7

真实的模型不可能每次都正确。它生成的数据可能是：

```text
3、2.8、3、2.7、2.9、3……
```

如果没有标准答案，也没有外部验证机制，训练程序就不知道哪些是 3，哪些是错误答案。它只能把这些内容全部当成学习材料。

于是，原来只存在于模型输出中的误差，被写进了下一代模型的参数。下一代模型再生成训练数据时，又可能在这些误差上产生新的偏差：

```text
第一代输出：3、2.8、2.7
                    ↓ 回灌训练
第二代输出：2.9、2.7、2.6
                    ↓ 再次回灌
第三代输出：2.8、2.6、2.5
```

这里的具体数字只是为了展示趋势，不代表所有训练都会按照固定幅度下降。真正的问题是：**错误一旦失去外部校验，就会从偶然输出变成训练目标；经过多轮回灌，偏差可能被保留、放大，数据也会越来越单一。**

这种退化常被称为“模型坍塌”。模型不一定在每一项能力上同时变差，但它可能逐渐遗忘真实数据中的少见情况，重复自己的表达习惯，并把原有偏差当成新的事实。

因此，更严密的说法不是“任何合成数据都会让模型变蠢”，而是：

> **如果模型生成的数据缺少独立验证，又被反复当成真实答案训练下一代模型，那么自训练只能复制已有信息，并有很大风险累积误差。**

---

## 五、有效迭代需要新数据和可靠反馈

可以把下一代模型的产生过程简化成：

```text
下一代模型 = 当前模型 + 新数据 + 可靠反馈
```

这里的加号不是严格的数学运算，而是在说明三个部分各自负责什么。

| 部分 | 作用 |
|---|---|
| 当前模型 | 提供已有的语言能力、知识和推理能力 |
| 新数据 | 提供当前模型原来没有见过的信息 |
| 可靠反馈 | 判断哪些输出正确、哪些错误，以及参数应该向哪里更新 |

只有新数据，没有可靠反馈，模型不知道应该学习什么、拒绝什么。

只有可靠反馈，没有新数据，模型可能把已有能力练得更稳定，却很难突破原来的信息边界。

只有当前模型自己的输出，既没有新信息，也没有独立判断标准，训练就容易变成一个封闭循环：

```text
模型相信自己的输出
  → 用自己的输出训练自己
  → 更加相信自己的输出
  → 继续用这些输出训练自己
```

一个说法被模型重复一万次，不会因此自动变成事实。

---

## 六、为什么有些“自我训练”确实有效

看到这里，可能会产生一个疑问：如果模型不能靠自己的输出进步，为什么围棋 AI 可以自我对弈？为什么模型生成代码和数学题后，也可以继续训练？

关键在于：这些系统并不是真的“只靠自己”。

### 围棋 AI 有胜负规则

AI 可以自己和自己下棋，但棋盘规则和最终胜负为它提供了独立反馈。一盘棋是谁赢了，不由模型凭语言感觉决定。

```text
模型生成棋局 + 规则判断胜负 → 可以学习
```

### 代码有编译器和测试

模型可以生成代码，但代码能不能编译、测试是否通过，可以由计算机实际运行后判断。

```text
模型生成代码 + 编译器/测试验证 → 可以筛选
```

### 数学证明可以使用形式验证器

模型可以尝试生成证明，形式系统则负责检查每一步是否符合规则。验证器不是根据模型“觉得对不对”来评分。

```text
模型生成证明 + 形式验证器检查 → 可以筛选
```

这些方法能够使用大量 AI 生成的数据，是因为系统外部存在明确规则、环境结果或验证工具。真正让训练有效的，不只是“生成了更多数据”，而是**生成之后还能可靠地分出对错**。

---

## 七、递归式自我改进：理论可行，但还没有完整实现

“自我迭代”最强的一层含义叫作**递归式自我改进**。

它设想的是：

```text
AI₁ 分析并改进自己的算法、参数或训练流程
  ↓
得到能力更强的 AI₂
  ↓
AI₂ 又能设计出更好的改进方法
  ↓
得到 AI₃
  ↓
不断循环
```

从理论上看，这件事并不矛盾。AI 可以辅助写代码、搜索模型结构、设计实验、生成训练数据和分析结果。未来也可能有系统把这些环节连接得更完整。

但“能够协助改进某个环节”和“能够独立完成递归式自我改进”之间，还有很长的距离。

一个完整闭环至少需要 AI 自主完成：

1. 找出当前系统真正的能力瓶颈；
2. 提出比现有方法更好的改进方案；
3. 修改代码、数据、训练流程或模型结构；
4. 获得训练所需的数据、算力和实验环境；
5. 判断新系统是真的更强，还是只在测试题上看起来更好；
6. 保证改进没有破坏其他能力和安全边界；
7. 让更强的新系统再次稳定地完成以上过程。

目前，AI 已经可以参与其中许多步骤，但还没有人实现一个完全自主、能够长期稳定运行并持续提升通用能力的递归式自我改进系统。

所以，它是一个理论上可以讨论、工程上正在接近部分环节，但尚未被完整实现的目标。

---

## 八、人类语料库是不是永远不可缺少

当我们说“AI 永远需要人类语料库不断供给”时，需要区分两个层面。

### 宏观层面：全人类社会的知识增长

人类语料库不是凭空出现的。它背后是人类对现实世界的观察、实验、生产、交往和记录。

例如：

- 新药有没有效果，需要临床试验；
- 新材料有什么性质，需要实际制备和测量；
- 某个国家今年的人口是多少，需要现实统计；
- 一项新政策产生了什么结果，需要社会运行后才能观察。

如果没有人类或其他系统继续接触现实、产生实验和观测数据，AI 只阅读旧语料、再生成新语料，就无法知道现实世界后来发生了什么。

因此，在宏观层面，真正不可缺少的不是“人类必须永远手写文章喂给 AI”，而是：

> **知识系统必须不断从现实世界获得新的信息，并用现实结果校准自己的判断。**

今天，这些信息主要由人类发现、整理并写入语料库，所以 AI 高度依赖人类语料。

### 特定领域：规则明确、结果可验证

在某些边界清楚的领域，AI 不一定需要人类持续提供大量新文本。

例如围棋有固定规则，代码可以运行测试，形式数学可以机械验证。AI 可以在这些环境中生成大量新案例，再根据外部结果学习。

但这仍然不是毫无依赖的自我循环，因为：

- 任务目标是预先设定的；
- 环境和规则提供了反馈；
- 正确性可以由模型之外的机制判断。

所以，特定领域可以减少对人类新增语料的依赖，却没有取消对外部规则、环境和反馈的依赖。

---

## 九、最后只保留这张图

```text
                     只在当前回答中重试
模型 ──→ 输出 ──→ 自我检查 ──→ 新输出
  │                                      不一定改变参数
  │
  └──→ 生成训练数据 ──→ 外部验证 ──→ 更新参数
                            │
                   规则、测试、实验、现实数据
                            │
                            ↓
                         新模型
```

最后可以把全文压缩成四句话：

1. **自我修正只是重新回答，不等于模型升级。**
2. **只学习自己的输出，正确内容不会凭空增加新知识，错误内容却可能被训练固化。**
3. **递归式自我改进在理论上可行，但完整、稳定、自主的闭环目前还没有实现。**
4. **AI 未必永远依赖人类文本，但一定需要来自自身之外的新信息和可靠反馈。**

真正的问题从来不是“AI 能不能生成更多内容”，而是：

> **谁来为这些内容提供它自己无法伪造的事实依据和对错标准？**

---

## 参考资料

- Shumailov et al., [The Curse of Recursion: Training on Generated Data Makes Models Forget](https://arxiv.org/abs/2305.17493)
- Silver et al., [Mastering Chess and Shogi by Self-Play with a General Reinforcement Learning Algorithm](https://arxiv.org/abs/1712.01815)
- Huang et al., [Large Language Models Cannot Self-Correct Reasoning Yet](https://arxiv.org/abs/2310.01798)

上一篇：[从 9 个参数的小模型，看懂什么是算法、什么是参数](./02-从9个参数的小模型看懂算法与参数.md)。
