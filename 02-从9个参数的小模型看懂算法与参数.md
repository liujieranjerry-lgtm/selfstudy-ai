# 02 — 从 9 个参数的小模型，看懂什么是算法、什么是参数

> 本文是 selfstudy-ai 自学笔记的第 2 篇。<br>
> 目标：不靠抽象定义，只用一个能手算完的小模型，看清算法、参数、反向传播、梯度下降和学习率分别负责什么，并把这些概念映射到 Decoder-only Transformer。

---

## 一、先记住一句话

> **算法规定“怎么算”，参数提供“拿什么数字算”。**

可以把一个模型压缩成下面这个公式：

$$
\hat y=f(x;\theta)
$$

- $x$ 是输入
- $f$ 是计算结构和步骤
- $\theta$ 是参与计算的全部可学习参数
- $\hat y$ 是模型算出的结果

这里最重要的是分号两边：

$$
f(x;\theta)
$$

$f$ 决定计算流程； $\theta$ 决定这个流程当前会表现出什么行为。

接下来，我们用同一个例子把它展开。

---

## 二、先搭一台只有 9 个参数的小机器

输入是一个二维向量：

$$
x=
\begin{bmatrix}
2\\
1
\end{bmatrix}
$$

这个模型有两层。

第一层：

$$
z=W_1x+b_1
$$

$$
h=\mathrm{ReLU}(z)
$$

第二层：

$$
\hat y=W_2h+b_2
$$

当前使用的数字是：

$$
W_1=
\begin{bmatrix}
0.5&-0.5\\
1&0.5
\end{bmatrix},
\qquad
b_1=
\begin{bmatrix}
0\\
0
\end{bmatrix}
$$

$$
W_2=
\begin{bmatrix}
0.2&0.8
\end{bmatrix},
\qquad
b_2=0.1
$$

先数一数：

| 参数 | 包含多少个数字 |
|---|---:|
| $W_1$ | 4 |
| $b_1$ | 2 |
| $W_2$ | 2 |
| $b_2$ | 1 |
| 合计 | **9** |

所以，这是一个只有 **9 个参数** 的模型。

注意，9 个参数不是 9 层，也不是 9 个矩阵。它的意思只是：模型里一共有 9 个能够通过训练改变的数字。

---

## 三、什么是参数：矩阵里那些会被训练修改的数字

在这个模型里，下面这些数都是参数：

```text
0.5、-0.5、1、0.5、0、0、0.2、0.8、0.1
```

训练开始前，它们会被初始化为某些数值。训练过程中，模型根据误差不断修改它们。训练结束后，真正被保存下来的主要就是这些参数。

参数可以理解成一个巨大公式里的“系数”。

例如：

$$
y=ax+b
$$

公式结构没有变化，但 $a$ 和 $b$ 不同，函数行为就不同：

$$
y=2x+1
$$

和：

$$
y=-3x+10
$$

使用的是同一种计算结构，却会得到完全不同的结果。

神经网络也是一样，只不过把 $a、b$ 换成了大量矩阵和向量中的数字。

### 哪些东西不是参数

在当前例子里：

| 数值 | 是不是参数 | 原因 |
|---|---|---|
| 输入 $x$ | 不是 | 每个样本都可能不同 |
| 中间结果 $z、h$ | 不是 | 它们是本次计算临时产生的 |
| 预测值 $\hat y$ | 不是 | 它是模型输出 |
| 损失 $L$ | 不是 | 它用来衡量错误大小 |
| 学习率 $\eta$ | 不是模型参数 | 它是人为设定的超参数 |
| $W_1、b_1、W_2、b_2$ | 是 | 它们会通过训练被修改并保存 |

因此，“参数”有两个关键特征：

1. 它参与模型的计算。
2. 它会根据训练数据被自动调整。

---

## 四、什么是算法：规定先算什么、后算什么

现在暂时遮住所有具体数字，只看步骤：

```text
输入 x
  ↓
计算 W₁x + b₁
  ↓
经过 ReLU
  ↓
计算 W₂h + b₂
  ↓
得到预测值
```

这一套计算顺序和规则，就是算法的一部分。

在日常 AI 讨论中，人们经常把模型结构和处理流程一起笼统地叫作“算法”。更严格地说，可以把它分成三部分：

### 1. 前向传播算法

规定输入怎样经过一层层矩阵、偏置和激活函数，最终产生预测。

当前模型的前向传播是：

$$
x\rightarrow W_1x+b_1\rightarrow\mathrm{ReLU}\rightarrow W_2h+b_2\rightarrow\hat y
$$

### 2. 训练算法

规定模型答错以后怎样学习：

```text
计算损失
  ↓
反向传播计算梯度
  ↓
优化器读取梯度
  ↓
根据学习率更新参数
```

### 3. 推理或生成算法

模型训练完成后，规定怎样使用已经学好的参数产生结果。大语言模型还会涉及 token 选择、采样和停止条件。

所以，“算法就是很多层矩阵组合”这个理解已经抓到了神经网络的主体，但还差两块：

- 矩阵之间还会穿插 ReLU、Attention、归一化等非线性或结构性操作。
- 训练时还需要损失函数、反向传播和参数更新规则。

更完整的说法是：

> **神经网络是一个由矩阵运算和其他操作组成的巨大参数化公式；算法规定公式怎样运行，参数是公式里会通过训练改变的数字。**

---

## 五、让算法真正运行一次

第一层先计算：

$$
z=W_1x+b_1
$$

代入数字：

$$
z_1=0.5\times2+(-0.5)\times1+0=0.5
$$

$$
z_2=1\times2+0.5\times1+0=2.5
$$

所以：

$$
z=
\begin{bmatrix}
0.5\\
2.5
\end{bmatrix}
$$

ReLU 的规则是：正数保持不变，负数变成 0。当前两个数都是正数，因此：

$$
h=
\begin{bmatrix}
0.5\\
2.5
\end{bmatrix}
$$

第二层继续计算：

$$
\hat y=0.2\times0.5+0.8\times2.5+0.1
$$

$$
\hat y=0.1+2+0.1=2.2
$$

这一次前向传播里：

- “先乘哪个矩阵、再经过什么操作”由算法决定。
- “矩阵里面具体是 0.2 还是 0.8”由参数决定。
- 计算机真正完成这些乘法和加法，需要算力。

---

## 六、同一个算法，换一组参数会发生什么

假设算法完全不变，输入也还是：

$$
x=
\begin{bmatrix}
2\\1
\end{bmatrix}
$$

我们只把第二层参数换成：

$$
W_2=
\begin{bmatrix}
0.4&0.6
\end{bmatrix},
\qquad b_2=0.1
$$

中间结果仍然是：

$$
h=
\begin{bmatrix}
0.5\\2.5
\end{bmatrix}
$$

但最终输出变成：

$$
\hat y=0.4\times0.5+0.6\times2.5+0.1=1.8
$$

算法没有变化，参数变化了，结果就从 $2.2$ 变成了 $1.8$。

这就是算法和参数最清楚的分界：

> **算法像固定的计算路线；参数像路线中每一个路口当前设置的数值。**

---

## 七、参数是怎样“学会”的

假设正确答案是：

$$
y=3
$$

模型当前预测：

$$
\hat y=2.2
$$

使用平方损失：

$$
L=\frac12(\hat y-y)^2
$$

得到：

$$
L=\frac12(2.2-3)^2=0.32
$$

损失 $L$ 告诉我们模型错了多少，但它没有直接告诉每个参数应该怎样修改。这一步由反向传播完成。

### 反向传播：计算每个参数对错误的影响

先计算损失对预测值的偏导：

$$
\frac{\partial L}{\partial\hat y}
=\hat y-y
=-0.8
$$

对于第二层第一个权重 $W_{2,1}$：

$$
W_{2,1}\rightarrow\hat y\rightarrow L
$$

链式法则给出：

$$
\frac{\partial L}{\partial W_{2,1}} =
\frac{\partial L}{\partial\hat y}
\frac{\partial\hat y}{\partial W_{2,1}}
$$

因为：

$$
\frac{\partial\hat y}{\partial W_{2,1}}=h_1=0.5
$$

所以：

$$
\frac{\partial L}{\partial W_{2,1}}
=(-0.8)\times0.5
=-0.4
$$

同理：

$$
\frac{\partial L}{\partial W_{2,2}}
=(-0.8)\times2.5
=-2.0
$$

因此：

$$
\frac{\partial L}{\partial W_2} =
\begin{bmatrix}
-0.4&-2.0
\end{bmatrix}
$$

这一步只是在计算梯度，还没有修改参数。

### 梯度下降：真正修改参数

人为设定学习率：

$$
\eta=0.1
$$

更新规则是：

$$
参数_{新}=参数_{旧}-学习率\times梯度
$$

于是：

$$
W_2^{新} =
\begin{bmatrix}
0.2&0.8
\end{bmatrix}
-0.1
\begin{bmatrix}
-0.4&-2.0
\end{bmatrix}
$$

$$
W_2^{新} =
\begin{bmatrix}
0.24&1.0
\end{bmatrix}
$$

偏置也更新为：

$$
b_2^{新}=0.1-0.1\times(-0.8)=0.18
$$

如果为了便于观察，暂时只更新第二层，新预测就是：

$$
\hat y^{新}=0.24\times0.5+1.0\times2.5+0.18=2.8
$$

预测从 $2.2$ 变成 $2.8$，更接近正确答案 $3$。

这里的职责必须分清：

| 环节 | 负责什么 |
|---|---|
| 损失函数 | 衡量模型错了多少 |
| 反向传播 | 计算每个参数的梯度 |
| 梯度下降 | 根据梯度修改参数 |
| 学习率 | 控制每次修改多少 |

一句话概括：

> **反向传播算梯度，梯度下降使用梯度，学习率控制步长。**

---

## 八、为什么训练会改变参数，却不改变算法

训练前和训练后，模型仍然按照同样的步骤计算：

```text
x → W₁x+b₁ → ReLU → W₂h+b₂ → 预测值
```

训练改变的是步骤中使用的 $W$ 和 $b$，而不是这条计算路线本身。

因此可以把训练理解成：

```text
算法保持不变
    ↓
拿训练数据反复运行算法
    ↓
根据错误计算梯度
    ↓
不断修改参数
    ↓
得到一组更合适的参数
```

当然，在研发模型时，人类也可能修改算法，例如：

- 把 ReLU 换成其他激活函数
- 增加或减少网络层数
- 改变 Attention 结构
- 更换损失函数或优化方法

这些属于修改模型结构或训练算法，不是普通训练过程中的参数学习。

---

## 九、参数、超参数和中间结果不要混在一起

三个词长得相似，但职责不同。

### 参数

模型从数据中学到的数字，例如：

$$
W_1、b_1、W_2、b_2
$$

### 超参数

训练开始前由人或调参程序设定的规则，例如：

- 学习率
- 批量大小
- 训练轮数
- 网络层数

超参数控制“模型怎样学”，但通常不是模型训练后保存的知识本身。

### 中间结果

一次前向传播临时算出的数字，例如：

$$
z、h、\hat y、L
$$

它们会随输入变化，不是模型长期保存的参数。

---

## 十、算力在这里扮演什么角色

算法和参数都不会自己运行。

刚才的前向传播需要真的完成矩阵乘法、加法和 ReLU 判断；训练时还要完成反向传播和参数更新。计算机执行这些数字运算的能力，就是算力。

所以三者可以这样区分：

| 概念 | 回答的问题 | 当前例子 |
|---|---|---|
| 算法 | 按什么步骤计算？ | 两层前向传播、损失、反向传播、更新规则 |
| 参数 | 计算时使用哪些可学习数字？ | $W_1、b_1、W_2、b_2$ 中的 9 个数字 |
| 算力 | 多快完成这些计算？ | CPU 或 GPU 执行乘法、加法和求导计算 |

更强的 GPU 通常只会让同一个模型算得更快，不会自动让模型多出知识。模型学到的统计规律主要体现在参数之间的组合关系中。

---

## 十一、把这个小模型放大到 Decoder-only Transformer

我们这个模型只有 2 个输入、2 层计算和 9 个参数。Decoder-only 大语言模型虽然拥有很长的 token 序列、很多层 Decoder Block 和数十亿参数，但最核心的逻辑没有改变。

### 1. 先看全局：整个模型是一个巨大的参数化函数

一个 Decoder-only Transformer 的前向计算可以先压缩成这张图：

```text
输入 token
  ↓
Embedding：把 token ID 变成向量
  ↓
Decoder Block 1
  ↓
Decoder Block 2
  ↓
……
  ↓
Decoder Block N
  ↓
最终归一化
  ↓
LM Head：把隐藏向量变成全词表得分
  ↓
logits
```

每个 Decoder Block 都是一个函数，整个模型则是这些函数一层套一层组成的巨大函数。上一层的输出是下一层的输入，最后一层的输出再交给 LM Head。

因此，前向传播不是某一层里的局部动作。它从 Embedding 开始，依次穿过所有 Decoder Block 和 LM Head，直到产生 logits；训练时还会继续计算损失。

```text
前向传播：
token → Embedding → Block 1 → … → Block N → LM Head → logits → 损失
```

反向传播同样不是只在某一层发生。它从损失出发，沿刚才的计算路径反向穿过 LM Head、最后一个 Block、前面的所有 Block，最终为各处参数算出梯度。

```text
反向传播：
损失 → LM Head → Block N → … → Block 1 → Embedding
```

梯度下降需要和前两者稍微区分。前向传播和反向传播会沿模型逐层计算；梯度下降不再把数据传过一遍模型，而是在梯度全部算好后，根据学习率更新各层参数。

```text
当前参数
  ↓ 前向传播
预测与损失
  ↓ 反向传播
全部参数的梯度
  ↓ 优化器根据学习率更新
新参数
```

所以可以说梯度下降**作用于整个模型的参数**，但不要说它像前向传播一样从第一层一路传到最后一层。推理或聊天时只执行前向传播和 token 选择，不执行反向传播与梯度下降。

### 2. 前馈网络：第二课的小模型怎样变成一个大函数

每个 Decoder Block 通常都包含 Attention 和前馈网络，也就是 FFN。第二课的两层小模型，正是 FFN 的缩小版：

$$
\mathrm{FFN}(x)=W_2\sigma(W_1x+b_1)+b_2
$$

这里的 $x$ 已经不是原始文字，也不是 token ID，而是当前 token 在这一层中的隐藏向量。矩阵 $W_1、W_2$ 和偏置 $b_1、b_2$ 是这一层 FFN 的参数。

现代大模型可能用 GELU 或 SwiGLU 代替 ReLU，也可能省略偏置，但主体仍然是“向量乘矩阵、经过非线性操作、再乘矩阵”。

在大模型尺度上，一个 FFN 本身就是拥有大量参数的大函数。但它仍然只是整个模型这个更大函数中的一个子函数，而且每个 Decoder Block 通常都有自己独立的一组 FFN 参数。

```text
整个模型
  ├─ Block 1：Attention + FFN 1
  ├─ Block 2：Attention + FFN 2
  ├─ ……
  └─ Block N：Attention + FFN N
```

### 3. 再看一层内部：QKV 怎样流向 FFN

以常见的 Pre-Norm Decoder-only Block 为例，一层内部可以先看成两段：先做 Attention，再做 FFN。

下面的 $X$ 代表当前这一层收到的整段隐藏状态。序列中的每个 token 都有自己的隐藏向量，因此每个 token 都会产生自己的 Q、K、V 向量；合在一起就形成这一层的 Q、K、V 矩阵。

```text
输入 X
  │
  ├─→ 归一化 → 生成 Q、K、V → Attention → 输出投影 W_O ─┐
  │                                                   │
  └─────────────────── 残差直连 ──────────────────────┘
                              ↓ 相加得到 H

H
  │
  ├─→ 归一化 → FFN ──────────────────────────────────┐
  │                                                  │
  └────────────────── 残差直连 ──────────────────────┘
                              ↓ 相加得到下一层输入
```

如果把归一化后的输入记作 $X'$，这一层会用自己的参数矩阵生成临时 Q、K、V：

```text
Q = X'W_Q
K = X'W_K
V = X'W_V
```

Q 和 K 用来计算“应该关注谁”，注意力权重再对 V 携带的信息进行加权汇总。真正送往后续步骤的是 Attention 汇总出的结果，而不是把原始 Q、K、V 三者直接塞进 FFN。

一层结束后得到新的隐藏向量。它会成为下一层的输入，下一层再用自己的 $W_Q、W_K、W_V$ 生成一套新的临时 Q、K、V。

```text
第 1 层输入 X₁
  → Q₁、K₁、V₁
  → Attention 1
  → FFN 1
  → 第 1 层输出 X₂

X₂ 作为第 2 层输入
  → Q₂、K₂、V₂
  → Attention 2
  → FFN 2
  → 第 2 层输出 X₃
```

每层的参数会被训练并长期保存；Q、K、V 和各层隐藏向量则取决于本次输入，是前向传播中的临时结果。

### 4. 输出投影、残差连接和归一化分别是什么

#### 输出投影

Attention 对 V 加权汇总后，还会经过一个可学习矩阵 $W_O$。这一步叫输出投影，它负责整理 Attention 汇总出的信息，并把结果映射回模型的隐藏维度。

$W_O$ 是当前 Attention 模块的参数。它不是模型最末尾预测 token 的 LM Head；每个 Block 都有自己的 Attention 输出投影，而 LM Head 位于所有 Block 之后。

#### 残差连接

残差连接就是把子层原来的输入直接加回子层输出。以 Attention 子层为例：

```text
H = X + Attention(Norm(X))
```

这条“直连道路”可以保留原始信息。即使某一层当前只学到很小的修正，原来的 $X$ 也不会被完全覆盖。

它还为反向传播提供了更直接的梯度通道，使很深的网络更容易训练。没有残差连接，几十层甚至上百层网络中的信息和梯度更容易在层层变换中衰减或失真。

#### 归一化

不同层产生的隐藏向量，数值大小可能逐渐变得不稳定。LayerNorm 或 RMSNorm 会把数值尺度调整到更稳定的范围，再交给 Attention 或 FFN。

归一化不负责理解文字，也不负责决定关注谁。它更像进入下一台机器前的“数值整理”，让各层接收到的输入保持在更容易计算和训练的尺度。

许多现代 Decoder-only 模型采用 Pre-Norm，也就是先归一化，再进入 Attention 或 FFN。不同模型可能调整具体位置，但当前可以先记住这两个式子：

```text
H = X + Attention(Norm(X))
X_next = H + FFN(Norm(H))
```

### 5. LM Head：最后怎样变成下一个 token

经过最后一个 Decoder Block 后，我们得到的仍然是隐藏向量，还不是文字，也不是 token 概率。LM Head 的任务，就是把最终隐藏向量转换成词表中每个候选 token 的得分。

```text
最后一层隐藏向量
  ↓
最终归一化
  ↓
LM Head
  ↓
全词表 logits
  ↓
Softmax：变成概率分布
  ↓
选择或采样
  ↓
下一个 token
```

LM Head 本质上仍是第二课已经见过的线性变换：

```text
logits = H_final W_vocab + b
```

假设模型隐藏向量有 4,096 个数字，词表有 50,000 个 token，$W_{vocab}$ 就负责把 4,096 维隐藏向量投影成 50,000 个得分。实际模型可能省略偏置。

这 50,000 个原始得分叫 logits。logits 还不是概率；经过 Softmax 后，它们才会变成总和为 1 的概率分布。推理时，模型再按照贪心选择、温度采样或其他生成规则选出下一个 token。

训练时，模型会把 logits 与正确的下一个 token 比较并计算损失。反向传播从这个损失开始，反向穿过 LM Head 和全部 Decoder Block。

LM Head 的 $W_{vocab}$ 是参数；最终隐藏向量、logits 和概率是本次前向传播产生的中间结果。部分模型会让 LM Head 与 Token Embedding 共享权重，但二者在计算流程中的职责仍然不同。

### 6. 哪些是参数，哪些只是临时结果

| 对象 | 类别 | 为什么 |
|---|---|---|
| Token Embedding 矩阵 | 参数 | 训练时更新并保存在模型中 |
| 每层的 $W_Q、W_K、W_V、W_O$ | 参数 | 决定这一层怎样生成并汇总 QKV |
| 每层 FFN 的矩阵和偏置 | 参数 | 决定这一层怎样变换隐藏向量 |
| 归一化中的可学习缩放量 | 参数 | 训练时会调整并保存 |
| LM Head 的 $W_{vocab}$ | 参数 | 把最终隐藏向量投影到词表得分 |
| 输入隐藏向量、Q、K、V | 中间结果 | 随本次输入重新计算 |
| 注意力分数和注意力权重 | 中间结果 | 由本次 Q、K、V 临时计算 |
| 各层输出、logits、概率 | 中间结果 | 属于本次前向传播 |
| 因果 Mask 和计算顺序 | 固定规则 | 属于算法，不通过训练改变 |
| 学习率 | 超参数 | 由训练者设置，控制更新步长 |

判断一个矩阵是不是参数，不能只看它是不是矩阵。关键仍然是：它是否会被训练更新，并作为模型的一部分长期保存。

### 7. 最后把完整流程串起来

```text
前向传播：

token
  → Embedding
  → [归一化 → QKV → Attention → W_O → 残差
     → 归一化 → FFN → 残差] × N 层
  → 最终归一化
  → LM Head
  → logits
  → 训练时计算损失 / 推理时选择 token

反向传播：

损失
  → LM Head
  → 第 N 层
  → ……
  → 第 1 层
  → Embedding
  → 得到全部参数的梯度

梯度下降：

优化器读取全部梯度
  → 根据学习率更新各层参数
  → 下一轮前向传播使用新参数
```

所以“70 亿参数模型”不是 70 亿层，也不是 70 亿个矩阵，而是 Embedding、Attention、FFN、归一化和 LM Head 等所有可学习矩阵与向量中，总共有大约 70 亿个数字。

这些参数不是一条条独立保存的句子。模型学到的语言规律和知识，通常分散在大量参数的共同作用中。

---

## 十二、关键概念速查表

| 概念 | 一句话解释 |
|---|---|
| 算法 | 规定输入按照什么结构和步骤变成输出 |
| 模型结构 | 规定有哪些层、矩阵和连接关系，是算法的重要部分 |
| 参数 | 模型通过训练学到并保存的数字 |
| 前向传播 | 使用当前参数，从输入一路计算出预测 |
| 损失函数 | 衡量预测与正确答案相差多少 |
| 梯度 | 损失对某个参数的局部变化率 |
| 反向传播 | 沿计算图反向使用链式法则，高效计算全部梯度 |
| 梯度下降 | 根据梯度方向更新参数 |
| 学习率 | 人为设定的超参数，控制参数更新步长 |
| 算力 | 执行矩阵乘法、反向传播等计算的能力 |
| Decoder Block | Decoder-only 模型反复堆叠的基本计算单元 |
| Q、K、V | 每层根据当前输入临时生成的查询、键和值 |
| 输出投影 | Attention 中用 $W_O$ 整理汇总结果的线性变换 |
| 残差连接 | 把子层输入直接加回输出，保留信息并帮助梯度传播 |
| 归一化 | 调整隐藏向量的数值尺度，使深层训练更稳定 |
| FFN | 每个 Block 内的前馈网络，是两层小模型的放大版 |
| LM Head | 把最终隐藏向量投影成全词表 logits 的输出层 |
| logits | 每个候选 token 的未归一化得分，还不是概率 |

---

## 十三、三个自测题

### 1. 把 $W_2$ 从 $[0.2,0.8]$ 换成 $[0.4,0.6]$，但计算步骤不变，改变的是什么？

<details>
<summary>查看答案</summary>

改变的是参数，算法没有改变。

</details>

### 2. 把 ReLU 换成另一个激活函数，但 $W$ 和 $b$ 暂时不变，改变的是什么？

<details>
<summary>查看答案</summary>

改变的是模型结构或算法，当前参数数值没有改变。

</details>

### 3. 换一张更强的 GPU，算法和参数会自动改变吗？

<details>
<summary>查看答案</summary>

通常都不会。改变的是执行同一套计算的速度和可承载规模。

</details>

---

## 十四、最后只保留这张图

```text
                 人规定
                    ↓
输入 ──→ [算法：怎样计算] ──→ 预测
              ↑
        [参数：使用哪些数字]
              ↑
      训练数据通过误差不断调整

算力：负责把上面所有计算真正执行出来
```

最简洁的总结是：

> **算法是机器的结构和运行规则；参数是机器内部通过训练调好的数字；训练就是在算法基本不变的情况下，不断寻找更合适的参数。**

---

## 参考资料

- [Deep Learning, Chapter 6: Deep Feedforward Networks](https://www.deeplearningbook.org/contents/mlp.html)
- [Stanford CS231n: Backpropagation, Intuitions](https://cs231n.github.io/optimization-2/)

上一篇：[从文本到 Token，再到向量](./01-从文本到token再到向量.md)。
