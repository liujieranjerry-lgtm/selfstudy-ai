# 01 — 从文本到 Token，再到向量：大模型的「阅读理解」全过程

> 本文是 selfstudy-ai 自学笔记的第 1 篇。  
> 目标：把 AI 视频里「文本 → token → embedding → 模型」那一套用通俗的语言讲透，同时补上视频没展开的 Transformer 完整结构：原始的 Encoder-Decoder 和现代 Decoder-only 的区别。

---

## 一、先记住一句话

> 大模型不认识文字。它只认识数字。

你打的字、说的中文、写的英文——在模型眼里全部是一串一串的数字序列。那文字是怎么变成数字的？分两步：**Tokenizer**（分词器）和 **Embedding**（嵌入层）。

---

## 二、Tokenizer：把文字切成「积木块」

### 2.1 切分 + 编号

假设你输入了这句话：

```
我喜欢吃西瓜
```

Tokenizer 拿到后做两件事：

**第一，切分。** 把句子拆成更小的单元。切分规则不是人写的，是模型在训练前通过 BPE（Byte Pair Encoding）算法自己学会的——它统计海量文本中哪些字经常挨在一起，自己决定把谁和谁绑成一个单元。

```
"我喜欢吃西瓜" → ["我", "喜欢", "吃", "西瓜"]
```

这里的每一个小块就是一个 **token**。它是大模型处理文本的最小单元。

**第二，编码。** 给每个 token 分配一个身份证号（token ID）。

```
"我"   →  ID 102
"喜欢" →  ID 450
"吃"   →  ID 387
"西瓜" →  ID 921
```

于是原本的一句话在模型眼里就变成了一串数字列表：

```
[102, 450, 387, 921]
```

### 2.2 Token 不等于「词」

很多人都以为 token 就是词，其实不是。

- 英文里：一个 token ≈ 0.75 个单词。"strawberry" 可能被拆成 "straw" + "berry" 两个 token
- 中文里：一个 token ≈ 1.5-2 个汉字

所以有人跟你说"这个模型支持 128K token"，你大概能换算——能装下大约 20-25 万汉字，相当于一本中等厚度的书。

### 2.3 Tokenizer 的另一半工作：解码

模型运算完后吐出来的也是数字——比如 ID 203。用户看不懂，所以 Tokenizer 要再出场一次，拿着这个数字去查词汇表，把它翻译回人类能看懂的文字（比如 ID 203 → "西瓜"）。解码时不需要切分，因为模型每次只吐一个 token，一对一翻译即可。

---

## 三、Embedding：给每个 Token 发一张「特征卡片」

有了 token ID，模型能识别"这是第 102 号字"了。但光知道编号没用——模型需要知道这个 token **在语义空间中的位置**。这就靠 Embedding。

Embedding 做的事：

```
Token ID 102 → 查一张巨大的"翻译表" → 一个 1536 维的向量

原始: "我" → 102
向量: [0.12, -0.47, 0.83, -0.21, 0.05, ...共1536个数字]
```

每个维度描述了这个 token 在某一个方面的属性——是名词还是动词、是积极的还是消极的、是具体的还是抽象的……没有人规定每个维度代表什么，全是训练中自己浮现出来的。

**关键认知：token 是身份证号，向量是这张证上面的"特征信息"。单凭身份证号别人不知道你是谁、多高多重；但特征向量把你每个维度都描述清楚了。**

---

## 四、Positional Encoding：给向量加上「位置感」

向量有了，但有一个致命问题：四个字 `[102, 450, 387, 921]` 进入模型时，模型不知道它们谁在前谁在后。它看到的就是四个向量，没有顺序概念。

但顺序对语义至关重要——

> "面，我吃了" VS "我吃了面"

如果模型不感知位置，它会把这两句话当成一模一样的一组向量。但实际上一个是强调"面"（被动句式），一个是普通叙事。整个意思完全不同。

解决方式是**位置编码（Positional Encoding）**：在进入模型之前，给每个 token 的向量加一个"第几个位置"的信号。

通常用的是正弦/余弦函数：

```
位置 0 → 加上 [sin(0), cos(0), sin(0/10000), cos(0/10000), ...]
位置 1 → 加上 [sin(1), cos(1), sin(1/10000), cos(1/10000), ...]
位置 2 → 加上 [sin(2), cos(2), sin(2/10000), cos(2/10000), ...]
```

这个信号振荡的节奏不同，能让模型分辨出「你在第几个位置」。加上位置编码后，"面，我吃了" 和 "我吃了面" 虽然 token 集合一样，但每个 token 的向量因为位置不同而不同——模型现在能区分两句话了。

---

## 五、但位置编码还不够——为什么需要 Self-Attention？

位置编码解决了「谁先谁后」的问题。但它没解决另一个更关键的问题——

**同一个词在不同的句子里，含义完全不同。**

举个例子：

- "我吃了面。" —— "面" 指食物
- "我们见个面吧。" —— "面" 指会面

如果只看 "面" 这个词自己的向量，模型根本不知道它在这句话里是哪个意思。这就需要让模型去看 "面" **周围的词**。

这就引出了 Transformer 的第一个核心操作——**Self-Attention（自注意力）**。它不是去看每个词的孤立含义，而是去看整句话里**每个词和其他所有词之间的关系**，通过这种关系重新理解每个词在这句话里的真实含义。

举个具体的例子。我们想理解句中 "吃了" 这个词的真实含义：

```
"我 吃了 面"
```

Self-Attention 做的就三步：

**步骤 1：每个词生成 Q、K、V 三张卡片。**

- Q（查询/Query）："吃了" 举着这张卡片问其他词——"你们跟我有关系吗？"

- K（键/Key）："我"、"面" 各自举着自己的身份标签——"我在这，我是主语 / 我是宾语"

- V（值/Value）：每个词携带的实际语义内容

这三个 Q、K、V 都是原始的 embedding 向量分别乘以三个**可学习的矩阵**得到的。矩阵里的数字就是咱们聊过的"参数"——训练时自己拧出来的。

**步骤 2：Q 去和所有 K 配对，算出注意力分数。**

本质就是矩阵乘法中的内积——对应位置相乘再加起来，和余弦相似度是同一种运算。

```
"吃了" 的 Q  ×  "我" 的 K   =  0.82  ← 高分！动作者
"吃了" 的 Q  ×  "吃了" 的 K =  0.15  ← 自己看自己，不重要
"吃了" 的 Q  ×  "面" 的 K   =  0.76  ← 高分！动作的承受者
```

**步骤 3：用这些分数加权混合所有 V。**

得分高的词（"我" 0.82、"面" 0.76）贡献大；得分低的（"吃了" 自己 0.15）贡献小。最终输出的不是一个孤立的 "吃了"，而是带上了上下文信息——知道「谁吃了」、「吃了什么」——的 "吃了"。

所以 Self-Attention 的全部秘密就一句话：**每个词通过和其他所有词算内积来重新理解自己，分数高的信息多带走，分数低的少带走。**

---

## 六、Transformer：Encoder-Decoder 完整结构

Self-Attention 只是 Transformer 里最核心的一块积木。完整的 Transformer 架构是为**机器翻译**设计的，分两个大模块：

```
输入："I ate noodles"
         ↓
  ┌─ Encoder（编码器）× N 层 ─┐
  │  每层 = Self-Attention     │
  │        + 前馈网络（Feed Forward）│
  │                           │
  │  Encoder 的 Self-Attention │
  │  是双向的——每个词能看到   │
  │  整句话的所有词（左和右） │
  └───────────────────────────┘
         ↓
  一个浓缩了整句话含义的"上下文向量"
         ↓
  ┌─ Decoder（解码器）× N 层 ─┐
  │  每层 = Masked Self-Attention │
  │        + Cross-Attention      │
  │        + 前馈网络             │
  │                              │
  │  最后输出一个字一个字地     │
  │  吐出："我 吃了 面"         │
  └──────────────────────────────┘
```

### Encoder 在干什么

把输入的整句话完整地"理解"一遍。它的 Self-Attention 是**双向的**——每个词可以看到它左边的词，也能看到右边的词。所有的词同时互相算关系、互相加权融合。结果是一份浓缩了整句话含义的中间表示。

你可以把它想象成：你读完一句话后在脑子里形成的那个概括性的理解。

### Decoder 在干什么

看着 Encoder 的那份"浓缩笔记"，一个字一个字地生成输出。它做三件事：

- **Masked Self-Attention**：和 Encoder 的 Self-Attention 一样，但加上一个「因果遮罩」——每个词只能看到自己**之前**的词，不能偷看后面的。因为生成时后面的词还没被吐出来，不存在。

- **Cross-Attention**：Decoder 里每个词除了看自己之前的词（Self-Attention），还会再去看一眼 Encoder 的浓缩笔记——"翻译到一半了，我再确认一下原文说了什么"。这就是 Encoder 和 Decoder 之间的信息通道。

- **输出层**：最后一层把向量翻译成词汇表里每个候选 token 的得分，选最高的往外吐。然后把这个刚吐的 token 接在序列后面，再走一遍整个 Decoder，循环直到吐出结束符。

---

## 七、现代大模型：只留 Decoder，砍掉 Encoder

上面说的是 2017 年原始 Transformer 的结构。但今天你用的 ChatGPT、Claude、Gemini、文心一言——它们**全部是 Decoder-only 架构**。

### 砍掉了什么

- **Encoder 整个砍掉了。** 没有双向注意力了，没有浓缩笔记了。
- **Cross-Attention 砍掉了。** 因为没有 Encoder 了，没东西可以 Cross。

### 只留 Decoder 怎么干活

GPT 的任务不是翻译，是**接话**——你说一句，它往下续。这天然就是 Decoder 的活（从左到右生成下一个 token）。Decoder-only 模型把"理解"和"生成"压在了同一条流水线里：

**第一阶段：Prefill（预填充）。** 把你的输入整体"吃进去"，用带因果遮罩的 Self-Attention（只能向左看）过一遍全部层。输入里的每个词都和其他词算关系——"吃了" 知道主语是 "我"、宾语是 "面"。这一步就是"理解"的过程，只不过只能单向（因果方向），不能双向。

**第二阶段：自回归生成。** 每吐一个新 token，就把它接在序列末尾，整串再过一遍全部层，再吐下一个。循环直到吐出结束符。

### 原始 Transformer vs 现代 Decoder-only

| | 原始 Transformer | 现代 Decoder-only（GPT 等） |
|---|---|---|
| 理解怎么发生 | Encoder 用双向 Self-Attention 处理整句 | Decoder 的 Prefill 阶段用因果 Self-Attention 处理整句 |
| 理解和生成的关系 | 两条流水线，Encoder 做完交给 Decoder | 同一条流水线，先 Prefill 再逐 token 生成 |
| 能不能看到后面的词 | Encoder 可以双向看 | 任何时候只能看左边（因果遮罩） |
| Cross-Attention | 有——Decoder 每层都看一次 Encoder 的输出 | 没有——砍掉了 |
| 代表模型 | Google 的 T5 | ChatGPT、Claude、Gemini、Llama、文心一言 |

砍掉 Encoder 损失了双向理解的能力。但换来的是一个统一的架构——不需要 Encoder 和 Decoder 之间的信息交接，一条流水线从头跑到尾。当模型足够大、数据足够多时，这个简化被证明**完全值得**：模型通过"接话"这个行为，自己学会了理解、推理、翻译、总结……所有事。

---

## 八、完整的用户输入流程（以现代 Decoder-only 模型为例）

把上面所有步骤串起来，就是你打一句话到模型回答的全过程：

```
你输入："今天天气"
        ↓
 ① Tokenizer（编码）
    "今天天气" → 切分 → ["今天", "天气"]
    → 映射 → [ID_231, ID_882]
        ↓
 ② Embedding
    [ID_231, ID_882] → 查表 → 两个 1536 维向量
        ↓
 ③ Positional Encoding
    向量 + 位置信号（正弦函数，标记第 0 位和第 1 位）
        ↓
 ④ Decoder × 96 层 —— Prefill 阶段（理解）
    每层 = Masked Self-Attention + 前馈网络
    "今天" 能看到自己，"天气" 能看到 "今天" 和自己（因果遮罩）
    两个 token 互相算注意力、加权融合，形成上下文语义
        ↓
 ⑤ 最后一层取 "天气" 位置的向量 → 翻译成 5 万个候选得分
    → Softmax → 选最高分 → 吐出第一个新 token "真"
        ↓
 ⑥ 自回归循环（生成）
    "今天天气 真" → 再走 96 层 → 吐出 "好"
    "今天天气 真好" → 再走 96 层 → 吐出 "啊"
    循环直到吐出特殊结束符
        ↓
 ⑦ Tokenizer（解码）
    每个输出的 token ID → 查词汇表 → 文字
        ↓
 你看到："今天天气真好啊"
```

这一步一步就是 ChatGPT 每一次回答你的完整过程。没有记忆，没有理解，没有意识——只有一整套数学机器在你的每个问题里反复运转。

---

## 九、关键概念速查表

| 概念 | 一句话解释 |
|------|-----------|
| Token | 大模型处理文本的最小单元，不等于"词" |
| Tokenizer | 编码（文字→数字）+ 解码（数字→文字） |
| Embedding | 给 token ID 发一张"特征卡片"（高维向量） |
| Positional Encoding | 给向量加上位置信号，否则"面我吃了"和"我吃了面"分不清 |
| Self-Attention | 每个词和所有词算关系，加权融合，重新理解自己 |
| QKV | Query 提问 / Key 标签 / Value 内容——Self-Attention 的三张卡片 |
| Encoder | 原始 Transformer 的双向理解模块 |
| Decoder | 原始 Transformer 的单向生成模块 |
| Cross-Attention | Decoder 里回头看 Encoder 输出的那一步 |
| Decoder-only | 现代大模型（GPT/Claude）——砍掉 Encoder 和 Cross-Attention |
| Prefill | 用户输入一次性全部过模型，形成上下文理解 |
| 自回归生成 | 每吐一个 token 就接在序列后面再过一次模型 |
| 因果遮罩 | 只能看左边和当前位置，不能偷看右边 |

---

下一篇：[从 9 个参数的小模型看懂算法与参数](./02-从9个参数的小模型看懂算法与参数.md)。
