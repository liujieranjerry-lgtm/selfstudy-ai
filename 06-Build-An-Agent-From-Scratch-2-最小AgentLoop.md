# Build An Agent From Scratch [2]：最小 Agent Loop（知识版）

> 从“理论骨架”到“能跑的最小循环”。
> 原文：[Build An Agent From Scratch 2：最小 Agent Loop](https://www.tritium.work/2026/06/08/Build%20An%20Agent%20From%20Scratch%20%5B2%5D%EF%BC%9A%E6%9C%80%E5%B0%8F%20Agent%20Loop/)
> 本版只讲通用知识，不复制教学项目的实现；少量代码只是「合同或结构化输出」的例子，完整实现见文末原文。

## 结论先行

第五课（Harness 理论框架）解决的问题是：Agent（智能体，能自己观察、决策、行动去完成目标的程序）到底是什么。它给出了两个核心概念——Agent Loop 是心脏，也就是“感知 → 思考 → 行动”的循环；Harness 是包住这颗心脏的工程外壳，负责上下文工程与注意力管理。但第五课的边界也很清楚：它只有图纸，没有能跑的东西。循环、模型调用、工具系统都还是名词，系统还没有真正动起来。

这一课（第六课）就是把图纸变成第一栋能住人的小房子：实现一个最小但能跑的 Agent Loop。它只做三件事——模型想一步、宿主做一步、结果看一步——再用一个循环把它们串起来，直到模型给出最终回答。核心知识可以浓缩成一句话：**Agent 不是模型本身，而是“模型 + 宿主 + 工具”之间的一圈循环。**模型（这里指大语言模型，负责理解和生成文字的人工智能）负责想；宿主程序（真正运行循环、执行工具、管理历史的程序）负责做；工具（模型可以点名请求执行的外部动作，比如计算、查天气、读文件）负责与世界打交道；循环负责让每一步的观察重新进入模型的眼睛。换模型、换工具都不动循环，失败也照样写回历史。

## 核心框架：最小 Agent 的三块积木

第五课给了一张未来会慢慢拼满的大图纸，这一课先拼其中最小、也最核心的三块：Agent Loop、LLM Client、Tool System。三块各有各的职责，但必须放在一起看：

| 积木 | 它负责什么 | 缺了它会怎样 |
| --- | --- | --- |
| Agent Loop | 驱动“想一步、做一步、看一步”的循环，并决定何时结束 | 模型和工具各自孤立，系统无法运转 |
| LLM Client（模型调用层） | 和模型供应商说话，把各家格式的差异挡在边界外 | 循环被某一家供应商绑死，换模型就要重写 |
| Tool System（工具系统） | 登记工具、说明参数、执行工具、把结果写回历史 | 模型只能动嘴，不能动手 |

三块必须一起看：循环是心脏，模型调用层是血管，工具系统是手。连接它们的是一条贯穿始终的纪律——无论成功还是失败，工具结果都要写回历史，成为模型下一轮的观察。下面几条原则就按“先搭循环、再立边界、最后定纪律”的顺序展开，每一条都踩在前一条已经成立的基础上。

## 原则一：循环先于一切——Agent 是一圈“想、做、看”

建造从循环开始：没有这圈“想、做、看”的循环，模型和工具只是两块不说话的积木。

原则是什么：Agent 的核心不是某个模型，而是一个循环。让模型想一步（Think）；如果它要求调用工具，宿主程序就真正执行工具（Act），并把结果写回历史；模型看到结果后再想下一步（观察，observation，也就是工具执行后回到模型面前的结果，重新进入它的眼睛）。只有当模型不再要求调用工具、直接给出回答时，循环才结束；同时还要有一个最大轮数（maxTurns）兜底，防止模型永远不结束——轮数上限就是安全阀，到了就强制收尾。

它回答什么问题：模型和工具之间靠什么连起来？

一个最小循环长这样：
```text

while (true) {
  把历史和可见工具交给模型；
  if (模型要求调用工具) {
    把工具调用写进历史；
    执行工具；
    把工具结果写进历史；
    continue;  // 进入下一轮
  }
  把模型回答写进历史；
  返回最终回答；
}
```

这段伪代码在系统中的角色，是整堂课的心脏：它把“想一步、做一步、看一步”翻译成一句“一直循环，直到模型直接回答”。按执行顺序看：每一轮先让模型基于历史和工具列表想一次；如果模型点名了工具，就把“点了什么工具、参数是什么”记进历史，执行工具，再把执行结果记回历史，然后进入下一轮；如果模型没有点名任何工具，就把回答记进历史并结束。它重要的原因是，工具结果写回历史这一步，正是 Agent 和普通聊天机器人的分界线——模型必须“看到”自己动作的结果，才能继续推理。历史在这里指的是本轮任务里已经发生的全部对话和工具结果，也就是模型当前能看到的“记忆”。

判断标准：一次任务里，模型的每次工具调用都有对应的执行结果回到模型面前；循环有明确的终止条件（最终回答或轮数上限）；用户指令、模型回答、工具结果都在同一条历史里按顺序躺着。

## 原则二：模型调用要收进统一边界

循环有了，可循环不能认识每一家模型供应商——它每轮都要和模型说话，而各家供应商的写法都不一样。所以第二件事，是把“和模型说话”收进一个统一入口。

原则是什么：Agent 的主循环只依赖两个统一动作——一次性拿完整答案（非流式）或一段一段接收（流式，也就是模型边生成边把内容送过来，而不是憋到整段写完）——并且只认一种标准化的“模型回答”形状。不同供应商（提供模型服务的厂商）的字段写法、工具调用格式、流式事件类型，全部关在边界后面的翻译层里；换供应商只换翻译层，循环一行都不用改。

这个统一入口本身是一份合同（contract，双方都必须遵守的约定）：
```typescript

export interface LlmClient {
  complete(request: LlmRequest): Promise<AssistantMessage>;
}
export interface StreamingLlmClient extends LlmClient {
  stream(request: LlmRequest): AsyncIterable<LlmStreamEvent>;
}
```

这段合同在系统中的角色，是“模型调用层的标准形状”：任何能跟模型对话的客户端，都必须提供 `complete` 方法——接收统一格式的请求，返回一个“稍后到达”的标准回答（`Promise` 就是承诺，结果准备好后才兑现）；流式增强版再加一个 `stream` 方法，让答案边生成边到达，而不是等整段写完。按执行顺序看，循环只做两件事：要么调用 `complete` 等完整答案，要么从 `stream` 里一条一条收事件，最后拼成同一个标准回答。它重要的原因是，合同把“循环”和“供应商”彻底分开：循环只认 `LlmClient` 这个形状，OpenAI 怎么写、Anthropic 怎么写，都被关在具体实现里。

它回答什么问题：换一家模型服务，循环要不要重写？

判断标准：换模型供应商时，Agent Loop 的代码不动；流式和非流式最终都汇成同一个标准回答；供应商特有的字段名不出现在循环里。

## 原则三：工具要统一登记、说明、执行

入口把“跟谁说话”挡在门外了，可模型光会说话还不够——它得能动手。而动手的第一条规矩，是工具必须统一登记、统一说明、统一执行。

原则是什么：工具不是散落在代码各处的函数，而是统一登记、统一说明、统一执行的资产。每件工具都有四样东西：名字（模型点名用）、说明（模型判断何时用）、参数规则（模型该交什么格式）、执行方法（真正干活）。登记表（相当于前台登记簿，名字到工具的对照表）负责防止重名；执行器负责按名字查找、校验参数、真正运行，并把结果包装成标准消息写回历史。

一件工具的标准形状：
```typescript

export type AgentTool = {
  name: string;
  description: string;
  parameters: JsonSchema;
  executionMode?: "sequential" | "parallel";
  execute(args: unknown, context: ToolExecutionContext): Promise<ToolResult> | ToolResult;
};
```

这段合同在系统中的角色，是“所有工具必须长成的样子”：`name` 是模型点名用的名字；`description` 是给模型看的说明，相当于招聘启事，告诉它这个工具能干什么；`parameters` 是参数填写规则（schema，规定模型该交哪些参数、哪些必填、每个参数是什么格式）；`executionMode` 声明这个工具是排队执行还是可以并行；`execute` 是真正干活的方法。按执行顺序看，模型先看到由这些字段压缩成的工具目录，再按名字点名并提交参数；执行器先查登记表，再校验参数，最后调用 `execute` 得到结果。它重要的原因是，模型眼里的工具永远是一份目录而不是实现——它不需要知道工具内部怎么算，只需要知道“叫什么、有什么用、参数怎么填”。

它回答什么问题：模型怎么知道有哪些工具、怎么正确调用？

判断标准：模型看到的工具列表来自登记表，而不是散落在各处；参数校验失败会变成可见的错误而不是崩溃；工具结果（包括失败）都会写回历史。

## 原则四：失败是一等公民——错误也写回历史

工具会失败——而且经常失败。成败之间最关键的纪律，是让失败也成为模型能看到的观察，而不是一声让整个循环停摆的爆炸。

原则是什么：工具不存在、参数不对、执行抛错，都不能炸掉整个循环。它们会被包装成一条带失败标记（isError）的工具结果消息，写回历史，让模型在下一轮读到失败原因，自己决定修正参数、换一个工具，或者向用户解释。

失败消息的常见形状：
```typescript

{
  role: "tool",
  toolCallId: "call_123",
  toolName: "calculator",
  content: "Tool not found: calculator",
  isError: true
}
```

这段结构化输出在系统中的角色，是“失败的官方说法”：它不是一声崩溃，而是一条普通的历史消息。按执行顺序看，执行器发现工具不存在或执行抛错后，把工具名、这次调用的编号、错误内容装进这条消息，再打上 `isError: true` 的失败标记，写回历史；模型下一轮读到它，就像读到一次普通观察一样。它重要的原因是，工具失败太常见了——文件不存在、命令退出码非零、网络请求被限流——失败如果不进入上下文，模型就没有自我修正的机会；崩溃是设计失败，不是工具失败。

它回答什么问题：工具失败后，Agent 是崩溃还是继续？

判断标准：任何工具失败都有对应的失败消息进入历史；模型能读到“哪个工具、因为什么失败”；失败不会中断循环，也不会被悄悄吞掉。

## 原则五：执行可以并行，历史顺序必须稳定

循环会跑、模型边界立了、工具能动手了、失败也进历史了——可多个工具一起成功时，顺序还不能乱：性能可以并行，模型看到的上下文不能乱。

原则是什么：一批互不依赖的工具可以同时执行（并行，也就是多个工具一起开工）来省时间；但只要有一个工具声明自己必须顺序执行（一个一个排队，前一个结束才轮到后一个），整批就降级成排队。更重要的是：无论并行还是串行，写回历史的结果顺序都和模型发出调用的顺序一致。

它回答什么问题：多个工具一起调用时，怎么既快又不乱？

判断标准：同一批工具的结果按模型请求的顺序进入历史；依赖别的工具结果的工具不会被提前执行；并行只是提速手段，不改变模型看到的上下文顺序。

## 原则六：事件先行——让外界看见循环

循环、边界、工具、失败纪律都齐了，最后还有一个“看得见”的问题——外界怎么知道 Agent 进行到哪一步了？

原则是什么：Agent 每走一步——开始、某一轮、模型思考、调用工具、工具结束、整体结束——都广播一个事件（event，也就是“发生了一件事”的通知）。界面、日志、监控面板只要订阅同一条事件流，就能显示进度、记录日志、排查问题，不需要拆开循环改代码。另外，推理强度（reasoning，也就是模型在回答前多想一会儿的用力程度）这类控制项也放在模型边界：上层只说“这次想用力一点还是轻一点”，具体翻译成哪家供应商的字段，由调用层负责。

它回答什么问题：外部程序怎么观察和接入 Agent？

判断标准：一个任务从开始到结束，外界能通过事件知道当前在哪一步；界面和日志不修改循环内部就能接入；推理等控制项以通用语义表达，由边界层翻译。

## 三个关键认识

六条原则垒完了，把这一课收束成三个可以带走的认识。

第一，Agent 是循环，不是模型。模型负责想，宿主负责做，工具负责与世界打交道；三者靠“结果写回历史”串起来。判断一个系统是不是真 Agent，就看它有没有这圈循环——而不是看它用了多聪明的模型。

第二，边界的价值在“换”的时候才显现。模型调用、工具登记、事件广播各有各的边界，平时看不出区别；换供应商、加工具、接界面的时候，边界清不清楚立刻见分晓。

第三，失败是上下文的一部分。工具失败越常见，越要把失败变成模型可见的观察，让模型有修正的机会；崩溃不是工具失败的结果，而是设计失败的证据。

## 一组可以带走的问题

认识是给自己看的，清单是拿去用的。以后看任何“智能体”或“自动化助手”系统，可以拿这组问题检验：

- 这个系统有没有“想一步、做一步、看一步”的循环？工具结果会回到模型面前吗？
- 换一家模型服务，循环逻辑需要改吗？供应商的格式被挡在哪一层？
- 模型怎么知道有哪些工具？每个工具有没有名字、说明、参数规则？
- 工具失败后会发生什么？失败信息会写回历史让模型看到吗？
- 多个工具一起调用时，结果写回的顺序稳定吗？依赖其他工具结果的工具会被提前执行吗？
- 外界能看到 Agent 进行到哪一步吗？界面或日志需要改循环才能接入吗？
- 循环有没有终止保护？模型一直调用工具时，系统会不会永远转下去？

## 两个真实案例：Codex 和 Pi

清单问的是“有没有”，案例看的是“长什么样”。文章作者在动手前拆解了两个真实产品，这一课的每条原则都能在它们身上看到具体模样。

**Codex（生产级实现）示范了原则一、四、五、六。** 它的循环逻辑就是“模型要么返回工具调用，要么返回最终回答”：工具结果必须回填给模型（原则一）；非致命工具错误会被转换成模型可见的失败输出，而不是让整个流程崩掉（原则四）；工具可以并行执行，但输出以稳定顺序写回历史，避免模型看到的上下文顺序漂移（原则五）；它还有完整的事件与挂钩系统，外部程序可以在特定时刻观察或介入（原则六）。

**Pi（轻量级实现）示范了原则一、二、三、五、六。** 它的 Agent Loop 写得非常显式，一眼能看到“问模型、抽工具调用、执行工具、把结果追加进历史、下一轮”（原则一）；模型调用被收进清晰的 LLM 边界（原则二）；工具执行策略可配置——只要有一个工具要求顺序执行，整批就排队，因为并行可能让依赖其他工具结果的工具拿到不完整信息（原则五）；事件流是一等接口，界面和日志订阅同一条事件流（原则六）；工具的标准形状——名字、说明、参数、执行方式——也在它的实现里看得很清楚（原则三）。

## 延伸与原文

本版只讲通用知识：循环、模型边界、工具登记、失败即观察、顺序稳定、事件先行。具体的代码实现——最小循环的完整写法、翻译层的字段映射、工具注册表与执行器、运行示例——都在原文里：[Build An Agent From Scratch 2：最小 Agent Loop](https://www.tritium.work/2026/06/08/Build%20An%20Agent%20From%20Scratch%20%5B2%5D%EF%BC%9A%E6%9C%80%E5%B0%8F%20Agent%20Loop/)。原文所属系列是「从零搭建 Agent」；本课的详细版笔记（含逐行代码解读与运行输出）仍保留在同目录的 `06 - Build An Agent From Scratch [2]：最小 Agent Loop.md`。下一课会在循环和模型之间插入上下文管理器，解决“每轮都把所有历史原样塞给模型”的问题。
