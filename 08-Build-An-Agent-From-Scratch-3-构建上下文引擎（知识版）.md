# Build An Agent From Scratch [3]：构建上下文引擎（知识版）

> 信息变多之后，“让模型看到什么”本身就是一门工程。
> 原文：[Build An Agent From Scratch 3：构建上下文引擎](https://www.tritium.work/2026/06/09/Build%20An%20Agent%20From%20Scratch%20%5B3%5D%EF%BC%9A%E6%9E%84%E5%BB%BA%E4%B8%8A%E4%B8%8B%E6%96%87%E5%BC%95%E6%93%8E/)

## 结论先行

当一个 AI 智能体（Agent，能自主决定下一步做什么的程序）开始拥有工具——能读文件、执行命令、搜索网页——它的对话记录会迅速膨胀。工具的输出、命令日志、网页正文、错误信息，连同历史对话，每一轮都要被重新喂给模型。连命令失败时留下的错误输出、退出码（程序结束时的状态数字）和超时记录（超过时限被强制叫停的标记）也会混进历史——失败信息同样是上下文的一部分，不能被当作例外。

于是出现三个问题：记录太多，把模型的上下文窗口（模型一次最多能读多少材料的容量）撑爆；旧日志、旧错误把模型的注意力带偏，让它忘记当前真正要做什么；不断变长的输入还让成本和缓存都难以优化。这篇文章的核心知识是：给模型的输入不是越多越好，关键是建立一层“上下文引擎”，始终回答一个问题——模型这一轮到底应该看到什么？

答案不是一道简单的“删多少”的算术题，而是一套相互配合的机制：把完整记录与模型视图分开，把稳定内容放在前面，用统一的尺度度量用量，按预算决定何时压缩，把旧内容变成交接摘要而不是直接删除，让模型在任务途中自己折叠已闭合的段落，最后让每一个决策都留下可查的记录。

## 核心框架：上下文引擎的六个要素

把结论里的答案拆开，就是六个必须一起看的要素。先看整体框架，再按建造顺序逐条看原则。

| 要素 | 在系统中的角色 | 必须和谁一起看 |
| --- | --- | --- |
| 记录与视图分离 | 完整历史是事实存档，模型输入是工作视图；压缩只作用于视图 | 是所有压缩动作的前提 |
| 稳定提示词分层 | 让模型每轮稳定知道“自己是谁、在哪里、有什么工具” | 配合用量估算与缓存策略 |
| 用量估算 | 给所有输入一把统一、廉价的尺子 | 是预算的前提 |
| 预算与触发 | 决定“用到多少就该压缩”，并给模型留出回复空间 | 依赖估算结果 |
| 多级压缩 | 空间不够时的三级处理：启发式截断、交接摘要、动态折叠 | 由预算触发，且不破坏记录 |
| 元数据留痕 | 每一轮的压缩决定都成为可查记录 | 让前面所有层可检验、可回放 |

这六个要素必须一起看：没有估算，预算就是空话；没有预算，压缩只会来得太晚或太早；压缩若不区分记录与视图，就会把事实存档毁掉；而如果不留痕，上下文出了问题也无从判断。

下面七条原则，就按这张表的顺序展开：先分开记录与视图，再搭稳定提示词，然后度量、预算，最后是多级压缩与留痕。每一条都踩在前一条已经成立的基础上。

## 原则一：完整记录与模型视图必须分开

整套上下文引擎的地基，是在动手整理之前先立一条边界：把“事实存档”和“工作简报”分开。

原则是什么：把“Agent 自己的对话历史”和“模型每一轮实际看到的输入”当成两个不同的东西。**历史是事实记录，承担“尽量完整”的职责；模型输入是工作视图，承担“尽量有效”的职责。**所有截断、压缩、替换和重排都只发生在视图上；历史默认不被触碰，只有显式的手动压缩（明确发起一个动作，把完整历史里的一部分旧内容，正式替换成一段摘要）才允许把结果写回历史。

它回答什么问题：信息变多之后，哪些操作可以放心做而不伤害事实？

“视图”在这里指的是：**模型每一轮实际拿到的那份输入材料**——也就是经过上下文引擎整理后、真正递到模型面前的内容。它是一个“工作版”，不是原始存档。

完整记录可以理解为“对话 + 行动”的全记录：用户发言、模型发言、模型发起的工具调用、工具的执行结果，全都在里面。

通俗理解，完整记录是公司的原始档案柜，里面保存着所有对话、工具输出、错误信息，一条不删；而“视图”是每次开会（提交给模型）前从档案柜里整理出来的会议简报。简报可以根据这轮会议的需要裁剪、摘要、调整顺序——只放最近几轮、旧内容压成一段交接摘要、稳定信息放在前面——但档案柜里的原件始终不动。

所以截断、压缩、替换、重排这些操作，全部发生在“简报”这一层，档案（完整历史）默认不受影响。只有当系统明确执行“手动压缩”时，才允许把整理后的结果写回档案。**这样做的价值在于：模型可以放心地对输入做任何取舍，不必担心把事实记录弄丢；一旦发现某次处理有问题，原始历史还在，可以重新生成视图。**

- 这里需要区分手动压缩和自动压缩：自动压缩只改变这一轮的视图，历史记录被原样保留；而手动压缩则会修改完整历史，部分原始消息被做成摘要。
	自动压缩示例：在使用codex时，codex提示的“上下文压缩”。本机存储的历史记录并没有被改变，但是对模型来说能够让它做判断和理解的数据确实被改变了，被部分压缩成了摘要。

判断标准：任何自动压缩都不能破坏完整历史；写回历史必须是一次显式的、可被触发的动作；记录与视图的职责分离，是后续记忆、规划等能力继续演化的前提。

## 原则二：稳定内容在前，动态内容在后

档案和简报分开之后，第一件要搭的结构是简报的开头：让模型每一轮都能稳定看到“自己是谁、在哪里、有什么工具”，而不是从越来越长的对话里自己猜。

原则是什么：**把进入模型的内容按变化频率分层排序——最稳定的放最前面，最动态的放最后面。**系统提示词不再是一句“你是一个助手”，而是一个有顺序的拼装：默认行为规则、环境信息、工具列表、用户目标、任务状态，按稳定程度依次排列。

它回答什么问题：如何让模型每一轮都以最低成本知道“自己是谁、在哪里、有什么工具”，而不是从越来越长的对话里自己猜？

把现状结构化是关键。例如把环境信息固定成同一格式的便签，放在开场白里：
```xml

<environment_context>
  <cwd>/path/to/project</cwd>
  <current_date>2026-06-11</current_date>
  <timezone>Asia/Shanghai</timezone>
  <shell>zsh</shell>
</environment_context>
```

这段示例的角色，是把**“当前工作目录、日期、时区、命令环境”打包成固定结构。**执行顺序：系统提示词先给默认行为规则，再给环境背景，然后是工具列表和用户目标，对话正文永远跟在后面。

为什么重要：结构固定、位置固定，模型每轮看到的是同一块开头，这降低了它理解现状的成本；这种用尖括号标签给文本分块的格式（XML 风格），标签名直接说明每段内容的身份。

顺序稳定还有另一层收益：缓存更容易命中。缓存可以理解为：读者反复读同一本书时，把熟悉的开头记在心里，后面的内容怎么变，都不影响开头部分的复用。动态内容一旦混进稳定区，开头就不再相同，缓存也就失效了。

判断标准：变化越慢的内容越靠前；同一类内容每轮保持相同顺序；新增的动态内容只出现在尾部。

## 原则三：上下文是预算——先度量，再决策

简报的结构稳定了，接下来需要一把统一的尺子：不知道用了多少，就不知道什么时候该压缩；度量有了，预算和触发线才成立。

原则是什么：模型一次能读的内容有限，这个限制叫上下文窗口，可以想象成一张固定宽度的桌面，放进去的每块内容都要占地方。因此输入必须像预算一样管理：先统一计量，再决定怎么花。

它回答了什么问题：上下文是有限的预算，我该怎么知道用了多少、还剩多少、什么时候该压缩？

Token（可以粗略理解为文本的最小计量块）是计量的单位，但精确计量并不免费：不同模型各有自己的分词器（把文本切成 token 的机器），精确换算既贵，又把整套逻辑绑定到某一家的供应商（提供模型服务的厂商）上。所以好的做法是用一个稳定、廉价的近似公式代替精确分词：
```typescript

export function estimateTextTokens(text: string | undefined): number {
  if (!text) return 0;
  return Math.ceil(text.length / 4);
}
```

这段代码的角色是给任意一段文本提供一把统一又便宜的尺子。执行顺序：先看文本是否为空，为空直接记 0；否则数一遍字符数，除以 4 后向上取整，得到估算的 token 数。

它为什么重要：Agent 可能对接不同模型，度量一旦绑定某家分词器，换模型就要重写整套预算逻辑；**近似带来的误差，换来的却是稳定和低成本。**

完整估算还必须覆盖请求的全部构成，而不只是对话文本：系统提示词（写给模型的开场白和岗位说明）、消息历史、工具说明（描述每个工具是做什么的）都要计入，三者相加才是总用量。更进一步，如果供应商在上一轮已经返回了真实用量，就不要从零估算，而是用实测值校准，只补算新增加的部分。

预算不仅要定上限，还要留出“回复空间”。触发压缩的临界值是这样算的：
```typescript

get compactionTriggerTokens(): number {
  const thresholdTokens = Math.floor(
    this.options.contextWindowTokens * this.options.compactionThresholdRatio,
  );
  return Math.max(0, Math.min(thresholdTokens, this.availableInputTokens));
}
```

这段代码的角色，是计算“用到多少就该动手压缩”的触发线。执行顺序：先把窗口上限（contextWindowTokens）乘以触发比例（compactionThresholdRatio），得到第一个候选值；再和“扣除预留回复空间后真正可用的输入空间”（availableInputTokens）比较，取两者中较小的那个，也就是更保守的口径；最后确保结果不为负数。

为什么重要：窗口上限不能全部用来放输入，模型还需要空间写出回答；如果只按比例触发，可能把回复空间挤掉，取保守值就是给模型留后路。

判断标准：估算不求精确但求稳定；计量范围覆盖请求的全部构成；有实测反馈时以实测为准；触发线必须保证模型始终有空间回答。

## 原则四：截断要有方向，而不是均等丢弃

预算到线之后，第一级处理是“急救”：空间不够时，不能随机或均等地砍，而要按价值取舍。

原则是什么：空间不够时，不能随机或均等地砍，而要按“对当前任务的价值”取舍。取舍方向有三条：最近的上下文最值钱，因为刚读的文件、刚失败的命令、刚返回的工具结果，正是决定下一步动作的依据；早期用户指令必须保留，因为那是目标和约束；旧轮次里助手回答和工具输出的细节最可丢弃。

它回答什么问题：压缩发生时，保什么、丢什么？

操作上按轮次（turn）为单位处理。一轮可以理解为：从用户说一句话开始，到 AI 完成这一轮应答和全部工具调用为止的完整往返；对话由两种身份的消息组成——用户消息（人说的话）和助手消息（AI 的回答），工具调用（模型决定使用某个工具的记录）及其输入输出也混在其中。**默认策略是从最新一轮往回保留，直到用完“最近保留”的额度；更早的轮次不原样保留，只把其中所有用户指令挑出来继续留在上下文里。**

判断标准：用户目标优先于过程细节；最近的工具结果优先于旧日志；宁可丢弃过程，不可丢弃目标与约束。

## 原则五：旧上下文不是删除，是交接

但急救式的截断太粗糙——它可能丢掉旧轮次里真正重要的事实。所以第二级处理是：旧内容不删除，而是交接下来。

原则是什么：纯截断虽然保留了用户指令，却可能丢掉旧轮次里真正重要的事实——比如早期读过某个文件发现关键函数，或者跑过一次测试得到一个具体错误。所以更高级的做法是：**让一个临时派生的工作分身（worker）把旧内容整理成一段交接摘要（handoff summary），再放回模型视图。**

它回答什么问题：旧信息如何既不占满窗口，又不真正丢失？

执行时，系统构造一个“只做摘要”的请求：**分身模型的任务只是把旧上下文整理成下一轮能接住的记录，不携带任何工具，也就不会在压缩过程中产生新的副作用。**摘要生成后，以一条普通用户消息的身份进入视图，但内容开头明确声明自己的身份：
```typescript

{
  role: "user",
  content: "Context checkpoint summary for the next model:\n" + summaryText,
}
```

这段代码的角色，是规定交接摘要的入场身份：它是一条用户消息（role 为 user），但内容（content）以“Context checkpoint summary for the next model”开头，再接真正的摘要文本（summaryText）。执行顺序：旧消息先被移出视图，这条带声明前缀的摘要被放在原本旧内容的位置，后面再接最近保留的轮次。

为什么重要：模型需要能区分“用户刚下的新指令”和“上一任工作留下的检查点”，否则会把摘要误当成新要求；它也不伪装成助手旧回答，避免上下文失真。

这个机制还留下了分工空间：主模型负责推理和工具调用，压缩模型可以换成更便宜的模型，只负责整理交接。一次性压缩本质是“急救”，默认只改变本轮视图；只有显式的手动压缩才把结果写回完整历史。

判断标准：摘要必须保留用户目标与关键事实；摘要的身份必须被明确标注；自动压缩不改记录，手动压缩才改；整理与推理可以分开使用不同模型。

## 原则六：让模型自己决定哪些上下文可以折叠

交接摘要适合“快满了”的急救，但任务进行中，哪些段落已经闭环、哪些还在当前路径上，固定规则很难判断。所以第三级处理是：把判断权交给模型，让它按需折叠。

原则是什么：一次性压缩只适合“快满了”的急救。但任务进行中，哪些旧对话已经闭环、哪些还在当前工作路径上，固定规则很难判断——只有模型自己知道当前真正依赖什么。因此可以**给模型一个压缩工具和一条触发提示，由它主动选择时机，把已闭合的段落折叠成可复用的摘要块（block）。**

它回答什么问题：压缩的频率和对象，能不能比“满了再动手”更精细？

做法是：当上下文用量达到一定比例（原文示例是约一半，具体比例是实现选择）时，系统提示词里多出一句提醒：
```text

Estimated context is at or above ... tokens.
Before continuing, call compact_context ...
```

这段提示的角色，是在上下文快撑满之前给模型的一个“考虑折叠”的信号。执行顺序：用量达到触发比例后，提示被追加进系统提示词；模型读到后，自己选择合适的时机调用压缩工具（原文给它取名 compact_context）；真正的摘要仍由分身模型生成，主模型不手写。

为什么重要：**把“何时压缩”的判断权交给最了解任务状态的模型，而不是靠固定规则猜。**

折叠后的摘要块不是一段孤立的文字，而是一个带登记信息的条目：它记录自己覆盖了哪些原始消息、哪些工具调用、主题是什么、来源是自动还是工具触发。后续能否复用、能否追溯，都靠这份登记。

判断标准：何时折叠由模型基于任务状态决定，而不是机械地定时触发；摘要块必须登记覆盖范围，保证可追溯；折叠只作用于视图，不破坏记录。

这里的上下文折叠和原则三提到的自动压缩有什么区别？

| 对比项 | 自动压缩（预算触发） | 原则六：动态压缩（模型触发） |
| --- | --- | --- |
| 谁决定压缩 | 预算管理器：用量到线了就触发 | 模型自己：觉得某段已经闭环就触发 |
| 什么时候压 | 上下文快满了，做“急救” | 任务进行中，随时按需折叠 |
| 压什么 | 超出额度的旧历史，整批压成一段摘要 | 一段一段地压，只压已经闭合的段落 |
| 压多细 | 一次性处理 | 维护多个摘要块，每块登记覆盖了哪些消息和工具调用 |
| 判断依据 | 用量数字（还剩多少空间） | 任务语义（这段内容当前还依赖吗） |

## 原则七：每一次上下文决策都要留痕

压缩的各级处理都建好了，还差最后一块：每一轮到底发生了什么，要能事后查清。留痕不是单独一层，而是贯穿所有层的观察窗。

原则是什么：每一轮请求都要附带“上下文元数据”（metadata，即关于这次请求自身的信息）：有没有压缩、估算来源是近似还是实测、压缩前后大概用了多少、用哪种方式压缩。这些信息随请求一起走，界面、日志、诊断工具都能读到同一份事实。

它回答什么问题：当 AI 行为变得奇怪时，如何判断是不是上下文管理造成的？

判断标准：任何观察者都能回答“这一轮发生了什么”；压缩的触发原因与代价可回放；元数据与请求同生同灭，不依赖事后猜测。

## 三个关键认识

七条原则垒完了，把整套机制收束成三个可以带走的认识。

第一，给模型的输入不是越多越好。信息过多会带来注意力漂移——模型被旧日志、旧错误带偏，忘记当前真正要做什么。管理“模型看到什么”本身就是能力的一部分。

第二，上下文管理不是满了再清理的一次性动作，而是一套持续的分层机制：先分层组织稳定内容，再统一度量，按预算触发，用多级压缩处理，最后留痕。每一层都有明确职责，缺一层整套机制就会失真。

第三，好的机制以“可替换”和“可观测”为前提。度量不绑定某一家的分词器，压缩模型可以与主模型分工，每个决策都有记录——这让系统在模型升级、成本优化和行为排查时都保持主动权。

## 一组可以带走的问题

认识是给自己看的，清单是拿去用的。评价或设计一套上下文管理时，可以拿这组问题过一遍：

- 你能否区分“完整记录”和“模型当前看到的输入”？压缩发生时，记录有没有被动过？
- 你知道当前上下文大约用了多少吗？估算依据是什么，有没有用真实用量校准？
- 模型每一轮都能稳定看到“自己是谁、在哪里、有什么工具”吗？这些内容的顺序稳定吗？
- 空间不够时，你的取舍方向是什么？最近上下文、用户目标、旧细节，谁的优先级高？
- 旧内容是被直接删除，还是被转成交接摘要？摘要是否保留了目标和关键事实？
- 摘要是否明确标注了自己的身份，让模型知道这是检查点而不是新指令？
- 压缩是只在“快满了”时急救，还是任务途中也能按需折叠？折叠的范围有没有登记？
- 你能事后回答“这一轮有没有压缩、为什么压缩、省了多少”吗？

## 两个真实案例：Codex 和 Pi

清单问的是“有没有”，案例看的是“长什么样”。

Codex（OpenAI 的编程智能体）在真实产品中示范了多条原则：它把历史消息与模型上下文分开管理（原则一），把稳定内容前置以利用缓存（原则二），用低成本 token 估算和动态构造的初始上下文工作（原则三），并在上下文接近上限时让派生的 worker 模型生成交接摘要（原则五），压缩触发线还刻意采用更保守的口径，保证模型永远有空间回答（原则三）。

Pi 是另一款产品，也采用类似的估算与压缩思路，但它的压缩触发实现与 Codex 不同。本文作者最初参考 Pi 的实现，后来改用 Codex 的更保守口径——这本身说明一个可迁移的判断：不同产品对“何时压缩”可以有不同选择，而“留出回复空间、取保守触发线”是更稳的默认（原则三）。

原文的教学项目则把这套六层结构按“稳定提示词→估算→预算→截断→交接摘要→动态折叠→元数据”完整串起来接入 Agent 循环，具体模块划分与参数取值见原文。

## 延伸与原文

本版只提炼通用知识，不复制教学项目的实现；想对照完整代码、模块文件、具体参数（窗口大小、触发比例、保留额度等）时，请阅读原文。原文还预告了下一步：上下文引擎会演化为记忆系统的调度入口——短期记忆活在模型视图中，中期记忆靠压缩与摘要承担，长期记忆沉淀为可复用的知识与偏好。

原文：[Build An Agent From Scratch 3：构建上下文引擎](https://www.tritium.work/2026/06/09/Build%20An%20Agent%20From%20Scratch%20%5B3%5D%EF%BC%9A%E6%9E%84%E5%BB%BA%E4%B8%8A%E4%B8%8B%E6%96%87%E5%BC%95%E6%93%8E/)
