# Build An Agent From Scratch：先计划再行动（知识版）

> 先计划、再行动：让智能体稳定完成长任务。
> 原文：[Build An Agent From Scratch 5: Planning and solve](https://www.tritium.work/2026/06/17/Build%20An%20Agent%20From%20Scratch%20%5B5%5D%EF%BC%9APlanning%20and%20solve/)
> 本版只讲通用知识，不复制教学项目的实现；少量代码只是「合同或结构化输出」的例子，完整实现见文末原文。

## 结论先行

先简短回顾第九课。第九课（原系列第四篇：让智能体有记忆）做的是记忆系统（memory system），它解决一个问题：哪些事实值得留下、需要时怎么找回来。它让智能体拥有了一座档案柜——能压缩旧对话历史、保存阶段性结论、从长期记忆里召回项目约定，会话中断后状态还能恢复。但记忆有一条边界：它知道“我读过什么、记住了什么”，却不知道“我现在正在做哪一步”，也不判断“任务是否完成”。所以到第九课结束时，智能体会聊天、会用工具、记得住，但长任务仍然不稳：做着做着忘了最初目标，漏掉关键环节却宣布完成，已经做过的事过几轮又重做一遍。

这一课（原系列第五篇：Planning and solve）要做的事，就是在第九课的记忆之上，补上规划器（planner）这块“任务账本”。

具体来说：把用户目标拆成结构化步骤，每一步有状态、依赖、完成条件和证据，由系统统一保管；计划创建后先暂停，请用户审批，批准前不能执行会改变现实的动作；执行中每轮只推进当前最重要的一步，并按观察结果更新账本；计划没完成就不能收尾。一句话：第九课让智能体“记得住”，这一课让它“干得稳”。

这两课合起来，正好对应本文的核心知识：长任务的稳定性来自系统结构，不来自模型的自觉。记忆管“哪些事实值得保留”，计划管“目标是什么、做到哪一步、凭什么说完成”，两者一起，智能体才从“会聊天、会用工具”变成“能稳定推进长任务”。

## **计划的四个环节**：

| 环节 | 干什么 | 关键约束 |
| --- | --- | --- |
| Plan（拆解） | 把用户目标拆成结构化步骤，每步有状态和完成条件 | 计划是账本，不是一段文字 |
| Review（审批） | 计划创建后暂停，请用户审阅或修改 | 审批通过前，不能执行会改变现实的动作 |
| Solve（执行） | 每一轮只推进当前最重要的一步 | 按观察结果更新计划状态 |
| Replan（重来） | 计划不成立、步骤被卡住或目标改变时 | 显式修改计划，而不是假装没变化 |

每一步都有明确的状态：`pending`（等待执行）、`in_progress`（进行中）、`completed`（已完成）、`blocked`（被卡住）、`cancelled`（已取消）。这套状态加上迁移规则，就是状态机（state machine，指系统只允许对象处于若干明确状态之一、并按规则迁移）——它是任务账本的地基。

这五个状态不是摆设，后面的每条原则其实都在管它们的迁移。对应关系如下：

| 状态 | 由谁决定、何时迁移 |
| --- | --- |
| `pending`（等待执行） | 被设为当前步骤、开始推进时离开 |
| `in_progress`（进行中） | 通过受控入口修改；完成时离开 |
| `completed`（已完成） | 必须附上证据才能进入；一般不再离开 |
| `blocked`（被卡住） | 步骤受阻时显式标记，再由重来环节处理 |
| `cancelled`（已取消） | 目标改变或计划替换时进入 |

另外还有一个状态不在步骤上、而在整个计划上：审批状态（review status，待审批或已批准）。它由原则五（结构变化退回待审批）、原则六（批准前不能动手）和原则七（护栏只在已批准且仍有开放步骤时生效）共同管理。

## 原则之间的递进关系：从地基到屋顶

九条原则不是九张互不相干的卡片，而是一条建造顺序：每一条都假设前面的已经成立，再回答下一层的问题。主链分四层，另有两条补充原则。

阅读顺序就是建造顺序：先有账本（一），再定账本的内部规矩（二、三、四），再放人的闸门（五、六），最后交给运行时监督（七）；八和九不是主链上的台阶，而是整个结构的两块旁板——八划清计划与记忆的边界，九决定整套机制怎么装进去。后面每条原则的标题都标了它所在的层。

## 原则一：计划是账本，不是作文

九条原则里，这一条站在最底下，是整座结构的地基。计划以什么形态存在，决定了后面所有规则能不能落地。

原则是什么： 让智能体“先计划再行动”，不能只靠提示词（prompt，发给模型的指令文本）里写一句“请一步一步想”。计划必须是一个结构化状态对象：每一步有编号、状态、依赖、完成条件、证据，由系统统一保管，而不是模型输出的一段话。

它回答什么问题： 为什么“提醒模型多想几步”不足以解决长任务？因为如果计划只是对话里的一段文字，它可能被上下文压缩掉；系统无法逐项检查进度；无法阻止模型提前说“完成了”；无法与权限、审批、会话保存联动；也无法表达“这一步凭什么算完成”。会话（session）指一次持续进行的对话，计划必须跟着会话保存下来，中断后还能恢复。

判断标准： 计划能不能被程序逐项检查？状态变了系统是否知道？会话中断再恢复，计划还在不在？如果答案是“否”，那它只是一篇作文，不是账本。

账本要解决的三种典型失败，正好对应长任务的三个坑。第一是任务漂移：智能体执行几轮后，注意力被最近的工具结果（tool result，工具执行后返回给它的信息）牵着走，只顾眼前，忘了最初目标——比如本来要写完整报告，读着读着变成只总结材料。第二是假完成：它生成一份看起来很完整的最终答复，其实漏掉了关键环节，比如没有核对结果就宣布完成。第三是重复劳动：已经读过、做过、确认过的事没有记成结构化进度，于是过几轮又重新做一遍。它不是笨，只是手里没有一份可靠的账本。

## 原则二：账本可以被看，不能被乱改

原则一刚刚把计划从一段文字变成一份账本，问题立刻跟着来：账本立起来了，谁能动它、怎么动才安全？

原则是什么： 计划状态的读取与修改必须分开。任何人都能查看账本，但所有修改只能通过受控入口——接口（interface，系统约定好的唯一入口）——进行，而且每次修改都会把修订号（revision，版本号）加一。负责记账的规划器本身不思考、不执行工具，只做纯状态管理：记账的和执行的分开，规则才不会被执行过程中的偏差污染。

它回答什么问题： 如果状态可以被随便改，所有校验都形同虚设——模型可以绕过“完成必须有证据”直接改状态。模型要动账本，只能通过系统提供的计划工具提出请求，由系统校验后再改；读取得到的是副本，不能顺着读到的结构绕开校验。

判断标准： 读取是否只暴露副本、不暴露内部结构？任何一次改动之后，界面、调试工具和系统能否立刻知道“计划变过了”？修订号的意义正在于此：它让“计划是否变化”成为可查的事实，而不是靠肉眼比对。

## 原则三：不能跳过依赖

账本有了，修改也有了规矩，但步骤之间还有先后。上一条管的是怎么改，这一条管的是按什么顺序推进——两者同属账本内部规则。

原则是什么： 当某一步声明依赖其他步骤时，被依赖的步骤必须全部完成，才能推进当前步骤。依赖（dependency）就是“做 B 之前必须先做完 A”这种先后关系。

它回答什么问题： 长任务里步骤通常有先后：先调研再写结论，先写代码再测试。如果模型可以任意跳步，顺序就失去了意义，后面的步骤会在空中楼阁上完成。

判断标准： 系统在启动或完成某一步时，是否真的检查了依赖列表？检查是强制拦截，还是只写进提示词请模型自觉遵守？

## 原则四：完成必须有证据，自证不等于证实

改法定了，顺序也定了，账本内部还差最后一条规矩：怎样才算真的完成？

原则是什么： 任何步骤要从“进行中”变成“已完成”，必须附上证据（evidence，可回溯的观察结果，如“文件已写入、测试通过”）。没有证据的完成会被系统拒绝。

它回答什么问题： 怎么区分“我觉得做完了”和“任务真的做完了”？智能体最常见的坏习惯，就是把前者当成后者。要求证据，是强迫它把“完成”落到可追溯的事实上。

判断标准： 每一步完成时，系统是否强制要求证据？证据是否具体到可以回去核对，而不是“已按计划完成”这种空话？

这里有一个必须诚实面对的天花板：如果写证据和审证据的是同一个智能体，它完全可以自己给自己“盖章”。所以证据机制的价值在于提高造假成本、让过程可回溯，而不是彻底杜绝造假；需要严肃把关时，应该引入独立的核验者——人，或另一个程序。

完成一步时，账本上应记录的内容大致是：
```text

step: 更新使用说明
status: completed
evidence:
  - 已写入使用说明的对应章节
  - 测试通过
```

这段内容在系统中的角色，是“完成凭据”。执行顺序是：智能体做完这一步，系统检查证据字段是否为空，为空则拒绝，非空才允许状态变成 `completed`。它之所以重要，是因为把“完成”从主观判断变成了可审计的记录。

如果模型想不带证据就标记完成，系统不会假装成功，而是把失败作为观察结果（observation，工具执行后系统看到的事实）返回：
```text

Completed plan steps must include evidence.
```

这条提示会作为工具结果回到模型手里，让它下一轮修正自己的调用。它之所以重要，是让错误成为模型看得见、改得了的输入，而不是直接中断任务或把错误抛给用户。

## 原则五：结构变了，审批作废

账本内部的事到这儿就齐了，主链进入第二层：人的控制。计划要过审批，那么已经批准的东西被改动时，批准还算不算数？

原则是什么： 计划通过审批后，如果只是更新证据或状态，不需要重新审批；但如果改了步骤标题、描述、依赖关系或完成条件，说明计划本身变了，审批状态要退回“待审批”。

它回答什么问题： 什么程度的修改必须重新征求用户同意？目标怎么拆、依赖怎么排、怎样才算完成，这些决定用户批准的对象；它们一变，旧的批准就失去了依据。而执行过程中的进度更新不改变计划本身，不必打扰用户。

判断标准： 系统能否区分“进度变化”和“结构变化”？对结构变化的处理是重新审批，而不是静默接受。

## 原则六：权限先于开放

审批状态既然存在，下一个问题顺理成章：在批准之前，智能体到底能做什么？

原则是什么： 在计划被批准之前，智能体只能用“读”类能力——看文件、搜索、查资料；不能使用“写”和“执行”类能力——改文件、写记忆、运行命令。而且这一条不是靠提示词提醒，而是运行时（runtime，系统实际运行的框架层）真的改变工具清单：计划没批准时，写入和执行类工具在请求里根本看不到，硬要调用也会被拒绝。

它回答什么问题： 怎样防止智能体在用户还没看过计划时就动手？读文件、搜索是低风险的信息收集，计划前应当允许，这也能避免“完全不了解环境就瞎计划”；写文件、跑命令是高风险的变更，计划批准前必须挡住。如果只靠“请模型自觉”，模型可能创建计划后立刻执行，计划就白做了。

判断标准： 审批前，会造成实际影响的工具是否真的不可用？审批通过后，它们是否自动恢复可见？权限是否由计划状态决定，而不是由模型的心情决定？

工具契约（contract，系统为每类工具定义的能力边界）里，每个工具都登记自己的访问类型：
```typescript

type ToolAccess = "read" | "write" | "execute" | "planner";
```

这段代码在系统中的角色，是一张“能力登记表”：`read` 只看不动，`write` 会改东西，`execute` 会运行命令，`planner` 专门操作计划账本。执行顺序是：模型每轮发起请求前，系统先查计划状态，再按这张表过滤工具——没有计划或计划待审批时只留下读类工具和计划工具，计划批准后全部开放。它之所以重要，是把“哪些事现在可以做”从一句建议变成硬边界：权限由系统裁决，不由模型自觉。用户批准这个动作本身，也会作为一条观察结果进入对话历史，让模型下一轮看见“计划已批准”，再继续执行。

## 原则七：指令、快照、护栏，三层缺一不可

前六条把账本、内部规则和人的控制都立起来了，但模型还需要知道“什么时候用计划、怎么用、什么不能做”。系统用三层机制配合，三层各有各的职责。

**第一层：静态指令，回答“模型该怎么行为”**

系统把规则写进提示词：复杂任务先创建计划；计划创建后停下来把详情发给用户、请求审阅；审批通过前不执行写入和执行类工具；每完成一个真实子任务就更新对应步骤的证据；计划不对或受阻，先改计划再继续；计划还有未完成步骤时，不输出最终答复。指令回答的问题是“模型应该怎么做”，但只靠指令不够——模型可能忘记查进度，所以还要有第二层。

**第二层：动态快照，回答“模型每轮看见什么”**

系统每轮都把当前计划以快照（snapshot，某一时刻状态的完整照片）形式注入模型眼前，让它不用“记得去查”也能看见进度。快照包含目标、当前步骤、各步状态和审批状态（review status，计划处于待审批还是已批准）：
```xml

<current_plan revision="3" review_status="approved">
  <objective>Update docs to match the runtime.</objective>
  <current_step id="write" />
  <step id="inspect" status="completed">Inspect current runtime files.</step>
  <step id="write" status="in_progress" current="true">Patch README and chapter docs.</step>
  <step id="test" status="pending">Run tests and build.</step>
</current_plan>
```

这段内容在系统中的角色，是每轮递给模型的“当前进度卡”。执行顺序是：系统组装本轮请求，把进度卡追加到提示词末尾，模型看到目标、当前步骤和各步状态，再决定下一步。它之所以重要，是让模型始终面对现实，而不是面对自己对进度的想象。它也有代价：进度卡每次变化都会让提示词缓存（cache，系统为省算力而暂存的重复内容）失效，所以注入位置本身是“先保证正确、再优化成本”的设计权衡。

**第三层：收尾护栏，回答“什么时候才能结束”**

模型仍然可能太早说“做完了”。于是系统加了一道护栏（guard，拦截不符合条件的收尾动作）：当计划已批准但仍存在 `pending` 或 `in_progress` 的步骤（即开放步骤，还没结束的步骤）时，即使模型不再调用工具、直接输出最终答复，系统也不放行，而是把一条结构化消息塞回循环：
```text

Plan mode guard: the conversation cannot end while plan steps are pending or in_progress.
Continue the task, or update the plan by completing, blocking, cancelling, or revising the open steps before the final answer.
```

这段内容在系统中的角色，是“不允许提前收尾”的拦截通知。执行顺序是：模型本轮没有调用任何工具，系统检查计划是否已批准、是否还有开放步骤，有则合成一次读计划动作——合成（synthetic）指系统替模型生成的一次工具调用，模型没有主动发起，但系统把它当作模型行为回灌——然后循环继续。它之所以重要，是因为它不要求模型永远自觉、不打断整个循环、也不把错误抛给用户，只是把模型拉回现实。规划器也由此从“记录工具”升级为“行为约束”：它参与决定任务什么时候可以结束。

## 原则八：计划和记忆分工，骨架与血肉

接下来的两条不再顺着主链往上走，而是站在旁边补两块板。先划界：账本和档案柜，各存什么？

原则是什么： **规划器保存骨架，记忆保存血肉。**放进计划的是：当前目标、步骤拆解、正在推进的步骤、各步状态、依赖、完成条件、证据、阻塞原因。放进工作记忆的是：阅读结论、设计决策、排查记录、较长的命令输出摘要、以后可能需要召回的相关上下文。长期记忆只收纳跨任务可复用的经验。

它回答什么问题： 哪些信息该记在任务账本上，哪些该进档案柜？如果混放，短期进度会污染长期记忆，档案柜很快被任务噪声塞满；反过来，账本塞满细节也会臃肿到看不清进度。计划的存在反而保护了记忆：任务进度留在账本，可复用经验才进档案柜。

判断标准： 问一句“这是关于我正在做什么的信息，还是以后还要用的经验？”前者进计划，后者进记忆；只有跨任务可复用的经验，才值得进入长期记忆。

## 原则九：用“加层”而不是“改核心”

最后一条是元原则，回头打量整个结构：前面八条都成立之后，怎么把这些机制装进一个已经跑起来的系统，而不把它搅乱？

原则是什么： 加入规划能力时，系统没有把主循环改造成一个复杂的流程引擎（workflow engine，专门编排多步骤流程的框架），而是以正交方式叠加：新增几个计划工具、一段静态指令、一份动态快照、两道护栏，主循环本身保持简单，各机制可以单独开关。

它回答什么问题： 新能力怎样加入而不把系统搅成一团？正交（orthogonal）的意思是各机制各管一件事：指令管“何时用计划”，快照管“每轮看见进度”，闸门管“审批前不能动手”，护栏管“没做完不能收尾”。它们各自简单，组合起来才产生稳定的长任务行为。

判断标准： 新增能力时，是往核心循环里塞新分支，还是在旁边加一个可独立开关的模块？核心是否仍能用一句话说清？

## 三个关键认识

第一，长任务的稳定性来自系统，不来自模型的自觉。计划、审批、权限、护栏都由运行时持有，模型只是提议者和执行者。任何一环如果只写在提示词里，就只是“希望”，不是“保障”。

第二，完成是一个可核验的状态，不是一个感觉。“我说做完了”和“证据证明做完了”是两回事；而写证据与审证据若是同一个主体，就存在自证风险，重要任务的核验需要独立于执行者。

第三，计划与记忆是不同的时间尺度。计划管“现在做到哪一步”，记忆管“以后还能不能用”。放对位置，两者互相保护；放错位置，互相污染。

## 一组可以带走的问题

- 计划是结构化的账本，还是会被压缩、丢失的一段文字？
- 每一步是否都有明确状态、依赖和完成条件？
- 状态可以被查看，但修改是否只能走受控入口？
- 任何一次改动后，系统能否立刻知道“计划变了”？
- 每一步完成时，是否强制要求可回溯的证据？证据由谁核验，核验者能否独立于执行者？
- 计划的结构（目标、步骤、依赖、完成条件）变化后，是否重新征求用户同意？
- 审批通过前，会造成实际影响的动作是否真的不可用，而不只是“被提醒不要做”？
- 模型每一轮是否都看得见当前进度，还是必须记得主动去查？
- 还有步骤未完成时，系统能否阻止收尾？拦截方式是拉回任务，而不是中断或抛错？
- 正在保存的信息，属于“任务进度”还是“可复用经验”？放对账本或档案柜了吗？
- 新能力加入时，核心循环是否保持简单，机制能否单独开关？

## 真实案例：原则如何落地到工程

**OpenAI Codex**（OpenAI 的编程智能体）采用计划模式：复杂任务先由智能体生成计划，用户审阅批准后，才允许执行会改动代码或运行命令的动作；用户提出修改后，计划重新进入待审批。这正是原则五（结构变了，审批作废）和原则六（权限先于开放）在产品里的直接体现，也是“计划→审批→执行”循环的真实样本。

**写一篇长报告**——任何智能体都会遇到的任务：要完成它，需要先确认素材范围，再读材料，再提炼，再写作，最后核对落盘。如果没有账本，模型可能读着读着变成只总结材料（任务漂移），或漏掉核对就宣布完成（假完成），或重复读已经读过的文件（重复劳动）。这个场景演示了原则一（计划是账本）和原则四（完成必须有证据）：把每一步和证据记下来，任务才不会被短期注意力牵着走。

原文还预告了下一个阶段的问题：当同一个命令连续失败、文件反复修改却一直通不过测试时，系统如何发现自己“在原地打转”。这属于计划层之外的反思层，也恰好说明：计划负责记账，不负责诊断。

## 延伸与原文

本版只讲通用知识：计划为什么必须是状态机、审批与护栏为什么不能靠自觉、计划与记忆如何分工。教学项目的实现细节——代码结构、文件组织、具体工具名称、测试与启动方式——请直接读原文：

[Build An Agent From Scratch 5: Planning and solve](https://www.tritium.work/2026/06/17/Build%20An%20Agent%20From%20Scratch%20%5B5%5D%EF%BC%9APlanning%20and%20solve/)
