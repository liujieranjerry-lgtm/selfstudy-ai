# 04 — Agent 的能力边界：它为什么能做和为什么不能做

> 本文是 selfstudy-ai 自学笔记的第 4 篇。<br>
> 目标：区分 Agent 与 LLM 的分工，理解验证函数如何决定 Agent 的能力边界，并看懂如何构建完全无人工干预的 Agent。

---

## 写在前面

在讨论agent之前，考虑到读者的知识界限，我们必须先说清楚agent和llm的区别。在下文可能会出现agent和agent+llm混合出现，但都用“agent”一词来表达的情况。

通俗地理解，我们可以把agent想象成一辆汽车，而llm就是驾驶员。

- **LLM（驾驶员）**：负责理解、推理、生成方案，它提供里先验知识，有偏采样等等。它决定 Agent “想得聪不聪明”。
- **Agent（汽车）**：负责执行、调用工具、读写文件、运行命令、管理权限、控制“感知 - 决策 - 行动”循环、验证结果、请求授权。这些能力都来自于agent自带的工具、沙箱、验证函数等等。这些决定 Agent “能不能真正做事”。

真正完成任务的是 **Agent + LLM**：大脑提供行动能力，agent提供判断能力，两者合在一起，才是完整的 Agent 系统。而我们说的API，可以理解成一个管道，它负责把LLM和agent连接起来。在同一agent使用不同的API时，实际上就是不同的驾驶员在开同一辆车。（例如codex接入DeepSeek或者gpt5.6）而同一个LLM接入不同的agent也会有不同的表现，因为不同agent的工程能力也不同，或者工作方向也不同。比如codex和claude code都是偏向agent，配备的工具也都是以编程优先。而work buddy就是办公agent，以办公为主，接入的工具也是以办公为主。就像suv和家庭轿车，跑车的区别。定位不同。

## Agent 的本质定义：从工具到行动者

在讨论agent之前，我们必须要将agent与chatbot（比如豆包）做一个区分。

chatbot是你给它下达一个指令，大模型在响应用户指令后，反馈出结。（具体过程我们在上面的课程已经讨论过了。）

而agent与chatbot最大的不同就在于，Agent 是以目标为驱动、能够自主规划并执行多步行动的程序，在执行过程中可以调用外部工具改变环境状态，并在必要时向人类请求授权或补充信息。

普通 AI 是工具，Agent 是行动者。

1. 交互模式：chatbot是被动响应（问答）；Agent 是持续感知 - 决策 - 行动的闭合回路。
2. 与环境的关系：chatbot只读取输入并输出文本；Agent 不仅读取环境，还能 改变 环境（如调用 API、写文件）。
3. 决策主体：普通对话中，人类驱动每一步推进；而在 Agent 模式下，Agent 自主驱动任务，人类仅在关键节点（如授权、补充信息）参与。

## 适合 Agent 承担的工作核心特质

基于上述本质，适合交由 Agent 承担的工作通常具备以下四个核心特质：

1. 认知密集型信息处理：不仅是接触自然语言，而是涉及大量需要认知推理的信息处理（理解、提取、归纳、关联）。

   也就是说，不是说“能听懂人话”就行，而是要像分析师一样真正动脑子：看完一堆资料，能抓重点、做总结、看出信息之间有什么关系。

2. 动态复杂流程的编排核心：并非紧凑固定的自动化流程（如 RPA），而是适合在 存在动态变量的复杂流程 中，作为编排核心调用各类工具。

   换句话说，执行任务不是一条写死的流水线，比如“收到订单就自动发邮件”这种固定流程（当然，这种简单的工作agent也完全可以胜任）。而是当流程中会不断出现意外情况时，Agent 要能临时判断、灵活调用不同的工具，把整个流程串起来。

3. 开放性目标的自主规划：给定泛化目标，Agent 自主规划实现路径。这类问题类似于 NP 问题： 验证一个解是否正确（Verify）远比找到这个解（Solve）容易。Agent 依赖廉价且客观的验证函数（如代码跑通、漏洞复现）来实现迭代收敛。

   你只给它一个大概目标，比如“把这个 Bug 修好”，它自己想办法一步步去做。难点在于：判断“答案对不对”往往比“想出答案”容易得多。就像考试，看到标准答案能判断对不对，但自己做出来很难。所以 Agent 的关键不是一次猜中，而是能快速试错：试一下，看结果对不对，不对就换思路继续试，直到做对。

4. 受约束的自主决策：在决策规则可被定义、决策结果可被验证、且失误成本可控的场景下，进行大量中低风险的自主决策，实现 human off the loop。

   在规则清楚、结果能检查、就算错了损失也不大的前提下，让 Agent 自己做大量中低风险的判断，不用每件事都来问人。比如自动审核资料、自动安排会议时间，只要规则和边界定好，它就可以自己批量处理，人不用一直盯着。

## 为什么 Agent 适合解决开放式问题

在理解 Agent 为何适合开放性问题时，计算复杂性理论中的 P/NP 框架提供了一个精准的分析视角。

P/NP 问题的特点是： 验证（Verify）一个解是否正确，远比找到（Solve）这个解容易。

NP 问题的定义是：给你一个候选答案，你能在多项式时间内验证它是否正确。

但找到这个答案本身，可能需要指数级的时间。

怎么理解？

别人给你一张填好的数独，你按行、列、宫检查一遍有没有重复，几分钟就能确认对不对。但让你从空白棋盘自己填出答案，就可能要尝试大量组合，时间长得可怕。所以我们说，verify要不solve容易。这里的多项式时间指的是运行时间随输入规模增长得比较温和的，比如n²，而不是2ⁿ。

这个不对称性是理解 Agent 能力边界的关键。

Agent 在处理开放性问题时，其工作流程如下：

1. 接收一个泛化目标（如修复这个 Bug、找到这个系统的漏洞）
2. **利用 LLM 的先验知识和外部接收的信息源**，**在解空间中进行有偏的采样，生成一个候选解**
3. 通过验证函数判断候选解是否正确（测试用例通过了吗？漏洞能被复现吗？）
4. 若验证失败，将失败信息纳入上下文，调整策略，生成下一个候选解
5. 重复上述循环，直到验证通过或达到终止条件

讲完流程，得先讲讲“解空间”和“有偏的采样”的定义。

什么是解空间？比如让agent修复一个bug，可能的改法非常多：改这个变量、改那个条件、换一种算法、调整调用顺序等等，所有这些“可能的方案”合起来，就是一个巨大的解空间。而如果让 Agent 盲目尝试，等于穷举，效率极低，可能试到天荒地老。

那么这个时候就要引入“有偏的采样” 。“有偏”的意思是：Agent 不会把每个方案当成同等可能，而是根据已有知识判断哪些方案更有可能成功，然后集中生成这些方向的候选解。比如说，看到报错是“空指针异常”，LLM 的先验知识会让它优先怀疑“某个对象没有初始化”或“某个调用返回了 null”，而不是先去猜“CPU 频率不够”。

理解这一切后，我们就可以说

这个过程的本质是：在解空间中搜索，并利用验证函数来剪枝和迭代收敛。

这与 NP 问题的结构高度同构：**Agent不需要穷举所有可能，只需要找到一个能通过验证的解。**与 NP 问题不同的是，Agent 的验证函数是根据目标自生成的，也就是目标实现的条件由 Agent 来自己判定。因此，**这个验证函数 / 单元测试 / 漏洞复现条件的准确性，决定了 Agent 的能力边界。**

所以说

如果：验证函数便宜、客观：Agent 能快速迭代，能力很强，比如写代码、渗透测试、数据清洗。

如果：验证函数很贵、很主观、或根本不存在：Agent 就不知道自己做对没有，无法收敛，能力边界就出现了，比如战略规划、心理咨询、基础科研。

注意，这里的“便宜”和“贵”，指的是验证反馈是否快速并且答案是否客观。说的是验证机制，不是用llm花了多少token，花了多少钱。

**（这其实也是为什么大部分agent都是做coding的，因为coding的验证成本很低，跑一遍就能知道结果。）**

### 验证函数的存在且准确，是 Agent 能力的真正来源

这个框架揭示了一个工作流：

验证函数廉价且客观 → Agent 能快速获得明确的反馈信号 → Agent 能高效迭代收敛 → Agent 能自主完成开放性任务

反过来，这也精确地解释了 Agent 的失效边界： **当验证函数代价高昂、主观模糊或根本不存在时，Agent 就失去了迭代收敛的锚点。**

| 验证函数类型 | 典型工作场景 | Agent 适用性 |
| --- | --- | --- |
| 廉价且客观（毫秒级反馈） | 代码调试、渗透测试、数据转换 | 高度适合，可完全自主 |
| 有一定代价但仍客观（分钟 / 小时级） | 系统部署、自动化测试、文档审查 | 适合，需配置重试机制 |
| 代价极高（月 / 年级反馈） | 战略规划、政策制定、教育 | 不适合独立承担，可辅助 |
| 极度主观（无客观标准） | 心理咨询、艺术创作、外交谈判 | 不适合，缺乏收敛锚点 |
| 验证本身也是 NP 难 | 基础科学研究、法律裁决 | 不适合，验证与求解同阶 |

### 一个重要的精确化：Agent 比纯 NP 搜索更聪明

严格来说，Agent 的搜索过程并非纯粹的 NP 穷举，而更接近 启发式搜索（Heuristic Search）。LLM 的训练赋予了 Agent 强大的先验知识，使其在猜解之前就能排除大量不可能的方向，大幅压缩了实际需要搜索的解空间。

因此，更准确的表述是：

**Agent 在开放性问题中的能力，体现的是一种利用强先验知识压缩 NP 搜索空间、并依赖廉价验证函数实现迭代收敛的能力。 P/NP 是这个现象背后的理论骨架，而 LLM 的先验知识则是让这个搜索过程变得高效可行的核心引擎。**

### 推论：承担后果是 Agent 最难逾越的边界

这个框架还推导出一个现实意义深远的结论： 越是需要承担后果的工作，越难被 Agent 替代。

**承担后果意味着决策的验证函数反馈周期极长、代价极高，且往往涉及道德与情感维度。**Agent 可以提供分析和建议，但无法真正负责，而负责任本身，正是这类工作的核心价值所在。

## 四、 如何构建完全无人工干预 (Human Off the Loop) 的 Agent

当前的 Agent 常常会停下来等待人类（Human-in-the-Loop），原因通常是：信息缺失、权限不足或风险控制。要实现真正的完全自主化，需要从工程上消除以上三个障碍：

1. 把目标定义得足够完整（消除信息缺失）：通过 System Prompt 提前消解所有歧义。明确成功标准、各种分支情况的处理方式，将隐性的判断标准显式化。
2. 把所有权限提前注入（消除权限不足）：在启动时就将所有必需的工具、API Key 和账号凭证注入环境，确保 Agent 不会因权限受阻。
3. 设计异常处理策略（替代人工确认）：
   - 保守策略：遇不确定情况跳过并记录日志。
   - 置信度阈值：低于阈值的决策转入待审查队列，不阻塞主流程。
   - 沙箱先行：不可逆操作先在沙箱中模拟验证。
4. 加入自我监控和重试机制：配备验证函数（检验每步结果）、重试逻辑（失败后换策略）和明确的终止条件，避免陷入死循环。
