# Build An Agent From Scratch：与时代接轨（知识版）

> 与时代接轨：给智能体补上“手”和“做法”。
> 原文：[Build An Agent From Scratch 5.3：与时代接轨](https://www.tritium.work/2026/06/19/Build%20An%20Agent%20From%20Scratch%20%5B5.3%5D%EF%BC%9A%E4%B8%8E%E6%97%B6%E4%BB%A3%E6%8E%A5%E8%BD%A8/)
> 本版只讲通用知识，不复制教学项目的实现；完整实现见文末原文。

## 结论先行

第十课（计划系统）解决的问题是：长任务里的不稳定性。没有计划时，智能体容易犯三种毛病——被眼前信息牵着走而忘记最初目标（任务漂移）、看起来答完了其实漏掉关键环节（假完成）、因为没有结构化进度而反复做重复劳动。第十课的办法，是把计划从“一段文字”变成“一张任务账本”：目标拆成结构化步骤，每步有状态和完成条件；计划创建后先停下等人审批；每完成一步都要附上证据；计划不成立就显式修改；账本还没结清，不允许收尾。但计划也有一条清晰的边界：它管“目标是什么、现在做到哪一步、凭什么说完成了”，不管“一类事具体怎么做”，更不管“手能不能伸到外面的系统”。

把操作手册写进计划账本，就像把菜谱贴在工程进度表上：进度表只回答“做到哪一步了”，不回答“这道菜具体怎么烧”。往前数，第九课的记忆系统管“知道什么”，第十课的计划系统管“做到哪一步”，这一课（第十一课，系列第 5.3 章）在它们之上再垒两层：技能（Skill，把“一类任务怎么做”写成说明书）让“做法”变成可发现、可按需读取的资源；MCP（模型上下文协议，外部系统向智能体提供工具的通用接口标准）让“外部工具”变成可插拔、接口统一的来源。

一句话概括核心知识：一个成熟的智能体，不是把所有东西都预先塞进系统提示词——也就是每次对话开始时放在模型面前的总工作说明——而是让“事实”“计划”“做法”“工具”各归其位，只在需要时才进入眼前；新能力再多，也要服从原有骨架的规矩。

## 核心框架：一座一层层垒起来的房子

到第十课为止，房子已经盖好了大半：循环（原文叫 Agent Loop，智能体“观察—思考—行动—再观察”的回合）是地基，决定一步一步怎么转；工具是手，让它能碰到真实世界；上下文引擎是窗户，决定每一轮看到什么；记忆是仓库（第九课），存放可召回的事实；规划器是楼梯（第十课），决定目标怎么一步步推进。这一课在房子上加了两块新积木：技能负责“一类事怎么做”，MCP 负责“手能伸到哪里”。把各层摆在一起看：

| 层 | 它回答的问题 | 它管什么 |
| --- | --- | --- |
| 循环 | 每一步怎么走 | 观察—行动—再观察的回合 |
| 工具 | 手能伸到哪 | 可调用的操作 |
| 上下文 | 每一轮看到什么 | 当前请求的视图 |
| 记忆 | 记得什么 | 可召回的事实与状态 |
| 流程（本课新增：技能） | 一类事怎么做 | 可召回的工作方法 |
| 工具来源（本课新增：MCP） | 工具从哪来 | 可插拔的外部工具 |
| 规划 | 目标怎么推进 | 步骤与完成条件 |

这张表必须整体看，不能挑一行：只看工具不看流程，智能体“会做”但“没章法”；只看流程不看工具，有章法但手够不到；上下文和记忆决定它当下到底能看到什么、记得什么；循环和规划决定这些能力能不能被组织成一次完整的行动。更关键的是，新加的两层必须服从老规矩——同一套权限分级、同一条上下文纪律、同一种观察机制。

下面七条原则，就是按“垒房子”的顺序一条条讲的：先把做法放对位置，再让它可发现、按需读、分级触发，然后把手伸出去，最后收好边界、服从骨架。每一条都建立在前一条已经成立的基础上。

## 原则一：事实与做法分开存放

上一课给了智能体一张任务账本：目标拆成步骤、每步有状态和完成条件。可账本只回答“现在做到哪一步”，不回答“这类事具体怎么做”——做法是另一类知识，得给它一个自己的家。

**原则是什么：** 一个系统里有两类知识。一类是“是什么”的事实：昨天的状态、已经发生的事、当前的结果。一类是“怎么做”的流程：遇到某类任务时先做什么、后做什么、按什么标准判断。它们应该分开存放、分开召回。在原文的体系里，记忆管事实，技能管做法；两者都是“可召回的知识”，但召回方式完全不同——事实按内容找，流程按任务触发。

**它回答什么问题：** 为什么不能把所有操作手册都写进系统提示词？提示词是每次对话开始时放在模型面前的工作说明，手册越多，模型每一轮都要背着一大摞当前用不着的流程，既费资源又分散注意力。为什么不能把流程塞进记忆？记忆按“曾经是什么”组织，技能按“遇到这类事该怎么做”组织。把菜谱和购物清单混在一张纸上，买菜和做饭都会变慢。

**判断标准：** 一段经验如果描述的是“遇到某类任务，按这个顺序做”，它就是做法，该有独立的存放位置；如果描述的是“过去发生了什么、现在是什么状态”，它是事实，归记忆管。两者都没有明确归属，说明系统还没为这类知识准备位置。

## 原则二：没有入口描述，技能等于不存在

做法有了自己的房间，可房子盖得再齐，客人也得先看见门牌。模型在最开始看不到技能全文——它要怎么知道这间房间里有什么、值不值得进去？

**原则是什么：** 技能在最开始不展示全文，模型只能凭一句话判断“这个技能跟当前任务对不对得上”。所以每个技能都必须有一个入口描述（原文里 description 字段必填）；名字反而可以省略，没有名字就用文件夹名或文件名兜底（fallback）。技能文件开头有一段元信息（frontmatter，相当于书的扉页：书名和简介），原文的例子长这样：
```yaml

name: repo-review
description: Review a repository change for bugs and missing tests.
```

这段“扉页”在系统中的角色，是技能被发现之前唯一能被看到的部分。按执行顺序看：系统扫描技能文件时先读这段元信息；description 被放进可用目录，模型凭它判断当前任务是否匹配；正文只有在匹配之后才会被读取。它重要的原因是，没有 description 就没有可发现入口——技能写得再好，也是锁在抽屉里的手册，抽屉都找不到，内容等于不存在。

**它回答什么问题：** 如何让智能体在正确的时候想起正确的做法？答案不是把做法硬塞给它，而是给每个做法一个可检索的入口。入口描述就是技能的封面简介：路过的人只看这一句，决定要不要翻开。

**判断标准：** 只看入口描述这一句话，能不能判断“这是做什么的、什么时候该用”？能，入口合格；必须读完正文才能判断，说明描述失职，先改描述再谈别的。

## 原则三：先给目录，再读全文（渐进披露）

门牌让人找到房间，可房间里的书不能全搬到大门口。模型每轮能同时注意到的信息有限——全文一次堆上来，会不会反而淹没了真正相关的内容？

**原则是什么：** 系统默认只把每个技能的三个信息放进提示词：名字、位置、一句话描述。模型每轮看到的是“可用技能目录”，判断当前任务匹配某个技能时，才主动去读正文。原文里，这个目录长这样：
```xml

<available_skills>
  <instructions>When a task matches a skill description, read the skill file first, then follow its instructions.</instructions>
  <skill name="repo-review" location="/path/to/SKILL.md">Review a repository change for bugs and missing tests.</skill>
</available_skills>
```

这段目录页在系统中的角色，是“告诉模型有什么可以查，但不替它把书搬上桌”。按执行顺序看：模型每一轮先看到这份目录；它把当前任务和每个技能的一句话描述比对；一旦匹配，就按 location 指向的位置去读完整正文；读完后按正文组织工作步骤。它重要的原因在于上下文是稀缺资源：模型每轮能同时注意到的信息有限，塞得越满，真正相关的内容越容易被淹没。渐进披露的原则可以推广成一句话：任何信息，能晚进上下文就晚进，能不进就不进。

**它回答什么问题：** 上下文放不下所有手册，怎么办？答案是把“索引”和“全文”分层：索引常驻，全文按需。

**判断标准：** 系统提示词里是否只有“名字 + 位置 + 一句话描述”级别的索引？正文是否只在判定需要时才读取？两问都答“是”，才符合这条原则。

## 原则四：自动发现不等于自动执行——危险流程留给人类触发

目录有了，书也按需读了，还差最后一道把关：目录里的每一本，都该让模型自己伸手拿吗？有些流程还在试验，有些翻开就收不回去，有些必须由人亲眼看过才能动。

**原则是什么：** 技能默认可以被模型自动发现、自动选用；但也可以标记为“模型不可自动调用”（原文字段 disable-model-invocation）。一旦标记，它不进可用目录，模型看不到、选不了，只有人用显式命令（如 `/skill:名字`）才能触发。

**它回答什么问题：** 谁有权决定“现在启动这个流程”？常规做法——写文档、做检查、例行迁移——让模型自己判断没问题；但实验性流程、后果不可逆的流程、必须由人知情才能执行的流程，不该让模型自作主张。这个设计把调用权从“自动”收回到“显式”，相当于给系统留了一条由人把关的通道。

**判断标准：** 一个流程如果满足“还在试验”“后果不可逆”“人必须知情”中的任何一条，就该考虑移出自动目录，改成显式触发。判断依据不是流程难不难，而是：让机器自动启动它，你放不放心。

## 原则五：统一协议——让外部工具可插拔

到这一步，“做法”这条线就走完了：有地方放、有门牌、按需读、分级触发。可一个智能体光会照着手册做事，还是纸上谈兵——它得真的够到外面的系统。问题于是从“怎么做”转向“用什么做”：外部工具怎么进来？

**原则是什么：** 过去每接一个外部系统，都要在智能体内部为它单独写一套接入代码：参数说明、鉴权（验证身份和权限）、调用、错误处理。接得越多，核心越臃肿，最后变成一个“集成仓库”。MCP 的思路是：外部系统用同一种协议向智能体声明三件事——我有哪些工具？每个工具的参数说明（schema，相当于工具的输入说明书：要提供什么、长什么样）是什么？调用后结果怎么返回？智能体只按统一流程处理：启动外部程序、连上、列出工具、过滤开关、统一命名、登记进工具注册表（相当于前台登记簿，所有可用工具的名单）。整个流程长这样：
```text

for each configured server:
  create transport            # 建立与外部程序的通信通道
  connect client              # 连上
  listTools()                 # 问：你有什么工具
  filter enabledTools / disabledTools   # 按开关过滤
  normalize tool name         # 统一命名
  register route              # 登记进工具注册表
```

这段流程在系统中的角色，是“外部系统进入智能体的统一入口”。按执行顺序看：先为每个配置好的外部服务建立通信通道——常见做法是把它作为子进程启动（让外部程序作为独立的附属进程运行，双方通过标准输入输出这条管道对话）；连上后立刻问它“你有什么工具”；把返回的工具按开关过滤，把名字规范成统一格式；最后登记进工具注册表，让模型像调用本地工具一样调用它。它重要的原因，是把“维护每个外部系统”的成本从智能体身上卸下来、还给外部系统自己：接入一个新系统，只需它按标准声明自己有什么，而不是在核心代码里重写一遍集成。

**它回答什么问题：** 工具生态由谁维护？答案是：外部系统维护自己的工具，智能体只维护统一的插口。像 USB：每种设备自己实现接口，电脑不用为每个牌子单独开孔。协议统一之后，新系统接入的成本从“重写一遍集成”降为“按标准声明自己有什么”。

**判断标准：** 接入新系统时，是否只需要“告诉它启动方式 + 让它声明工具清单”，不需要为它写参数说明、鉴权、调用和错误格式化？是，说明工具来源已经协议化；每接一个都要在核心代码里新增一大块，说明还停留在手工集成时代。

## 原则六：外来工具要隔离、容错、善后

协议让手伸得出去，可门一开，进来的东西未必守规矩。外部系统有自己的状态、鉴权、限速，还会出错——进门的每一样工具，凭什么让我们放心？

**原则是什么：** 外部工具和内部工具不能完全混为一谈。原文做了三件事：第一，给每个外部工具的名字带上来源前缀——例如 `mcp_docs_server_echo` 一看就知道来自 docs 这个服务、原始工具叫 echo，重名时再追加一段由名字算出的指纹（hash）保证唯一；第二，外部工具默认顺序执行，因为外部系统可能有自己的状态、鉴权、限速和副作用，不能假设它们能安全地并行；第三，容错——外部服务启动失败、工具调用失败都不能把整个智能体拖垮，返回结果里的非文本内容（比如图片）不会塞满上下文，正文只提示一句“该工具返回了 N 个非文本内容块”（`[MCP tool returned N non-text content block(s).]`），细节收进附注。最后，系统关闭时要释放外部进程，像离开房间要关窗锁门。

补充一个细节：这类外部服务通常推迟到第一次真正需要时才启动（原文叫 lazy start），因为启动外部程序是异步操作——不等它全部就绪就先继续别的，之后回头取结果。这样调用方既可以预先准备好一个已启动的实例，也可以只交一份配置，让系统在运行时自己按需启动；两边的入口都能对上。

**它回答什么问题：** 开放边界带来的风险由谁兜住？统一协议解决“接得进来”，隔离与容错解决“出问题不至于全盘崩”。能进来的东西越多，越需要一套识别、降级、清理的纪律——开放不是放任。

**判断标准：** 三个问题都答“是”，才算有纪律的开放：外部工具出故障时，智能体能继续做完能做的事，而不是整体停摆吗？日志里能一眼看出某个工具来自哪个来源吗？关闭系统时，外部进程和连接都被释放吗？

## 原则七：扩展是加层，不是改核

新房间一件件盖好了，最后要把整座房子看一遍：这些新加的东西，是加建，还是把承重墙拆了重盖？

**原则是什么：** 无论接进来多少技能和外部工具，智能体的核心结构不变：模型仍然通过工具观察世界，上下文引擎仍然决定每轮看到什么，记忆仍然负责可召回的事实，规划器仍然负责目标的推进与完成。MCP 和技能只是让“工具来源”和“做法来源”变多，但新来源必须服从同一套权限治理（读、写、执行分级）、同一条上下文纪律和同一种观察机制。原文把这一点看作“与时代接轨”的真正含义：接轨不是追热点，是把新生态放进已有的骨架里。

**它回答什么问题：** 什么才算一次好的扩展？答案不是“功能多不多”，而是“扩展后骨架有没有被推翻重来”。新功能若需要单独开一套权限、单独绕开上下文管理，它就不是扩展而是补丁；若只是往已有层里增加新来源、服从旧规矩，它才是接轨。

**判断标准：** 问一句：把新能力拿走，核心结构还完整吗？把核心结构拿走，新能力还成立吗？前者答“是”、后者答“否”，说明扩展是加层；反过来，说明新能力长在了错误的层上。

## 三个关键认识

到这里，可以把前面七条原则收束成三个可以带走的认识。

第一，知识的价值取决于存放位置。事实进记忆、计划进规划器、做法进技能、工具进注册表、调用权分级——同样的内容放错层，要么找不到，要么挤占注意力，要么失去控制。

第二，上下文是稀缺资源，渐进披露是通用纪律。无论信息来自记忆、技能还是外部工具，都应该“需要时才出现”；能放索引就不放全文，能按需读就不预先塞。这条纪律不只在智能体里成立——任何信息密集的系统，先给目录、再按需取全文，都比一次全给更可靠。

第三，开放边界必须以纪律为代价。接外部系统靠统一协议，管外部工具靠来源前缀、顺序执行、容错与善后；扩得越开，骨架越要稳。真正“与时代接轨”的做法，不是每出现一个新概念就堆一个功能，而是让新能力在旧骨架里找到自己的层。

## 一组可以带走的问题

认识是给自己看的，清单是拿去用的。下次看任何带“智能体”“助手”“自动化”色彩的系统，都可以逐条问：

- 这段经验是事实还是做法？它有没有独立的存放位置？
- 一个做法的入口描述，能否只看一句话就判断何时使用？
- 系统默认携带的信息是不是最少的？正文是否只在需要时读取？
- 哪些流程不该由模型自己决定触发？有没有留给人的显式通道？
- 接入外部系统时，它能否用统一协议声明“有什么工具、参数是什么、结果怎么返回”？
- 外部工具在日志里能否被认出来源？出故障时会不会拖垮整体？
- 关闭系统时，外部进程和连接是否被释放？
- 新能力是否遵守已有的权限、上下文、状态规则，而不是另开一套？

## 真实案例：原文里的三个落地片段

清单问的是“有没有”，案例看的是“长什么样”。下面是原文中真实出现的小片段，每个都对应前面的原则；只讲它们示范了什么，实现代码见原文。

**一个“代码检查”技能（示范原则一、二、三）。** 原文给了一个名为 repo-review 的技能，入口描述只有一句话：“审查一次代码变更，找出缺陷和缺失的测试”；正文则规定“先读变更，按风险排序，先报告发现”。它示范的是：做法与事实分开存（原则一）、一句话入口决定可发现性（原则二）、正文不进提示词、需要时再读（原则三）。

**一个不让模型自己碰的流程（示范原则四）。** 技能开头可以标一句“模型不可自动调用”，它就不进自动目录，只有人用显式命令 `/skill:名字` 触发。适合实验性、有风险、或必须由人把关的流程。

**一个外部工具登记的例子（示范原则五、六）。** 假设某个文档服务提供了一个叫 echo 的工具，登记后模型看到的工具名是 `mcp_docs_server_echo`：前缀标明来源（原则六），协议统一声明参数与结果（原则五）；若返回里带图片等非文本内容，正文只提示数量，细节进附注（原则六的容错与降级）。

## 延伸与原文

本版只讲知识：事实与做法的分层、可发现性与渐进披露、自动与显式双通道、统一协议、外部工具的隔离容错与善后、以及“扩展是加层不是改核”。具体的实现——技能加载器的扫描规则、MCP 客户端的启动与工具映射、如何接入智能体主循环、测试与演示代码——都在原文里：[Build An Agent From Scratch 5.3：与时代接轨](https://www.tritium.work/2026/06/19/Build%20An%20Agent%20From%20Scratch%20%5B5.3%5D%EF%BC%9A%E4%B8%8E%E6%97%B6%E4%BB%A3%E6%8E%A5%E8%BD%A8/)。原文所属系列是《Build An Agent From Scratch》，同步项目在 GitHub（Singularity，stage 4.3 MCP + Skills 相关变更）。
